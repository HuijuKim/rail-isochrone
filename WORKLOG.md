# 작업 로그

세션이 바뀌면 대화가 사라지므로 여기에 남긴다. 무엇을 왜 고쳤는지와,
아직 안 끝난 것을 적는다. 코드가 왜 지금 모양인지는 HANDOVER.md 에 있다.

---

## 이름

사이트 이름을 "수도권 도달권역" 에서 **철도 등시선 (Rail Isochrone)** 으로
바꿨다. 권역이 간토·간사이로 늘어서 "수도권" 이 틀린 이름이 됐다.

    ko 철도 등시선 / ja 鉄道等時線 / en Rail Isochrone
    zh-Hans 铁路等时线 / zh-Hant 鐵路等時線

저장소 이름은 `rail-isochrone`. User-Agent 세 곳(`build_colors`,
`build_express`, `fetch_data`)과 README·REQUESTS 를 맞췄다.

GitHub 저장소는 `HuijuKim/rail-isochrone` 으로 바꿨고 원격 주소도 옮겼다
(옛 주소는 GitHub 가 넘겨 준다). **로컬 폴더는 아직 `tokyo-isochrone`
이다.** VS Code 가 작업 공간으로 열고 있으면 잠겨서 못 바꾼다. 창을 닫고
바꾼 뒤 `C:/Users/Huiju/rail-isochrone` 을 다시 열면 된다. git 설정은
폴더 안에 있어 따라간다.

브라우저 탭 제목이 `<title>` 에 한국어로 박혀 있어 언어를 바꿔도 안
바뀌던 것을 고쳤다. `data-i18n` 은 요소 안 텍스트만 갈아 끼우는데
`<title>` 은 그 대상이 아니다.

---

## 선형 (지도에 그리는 선)

### 구간 선형을 따로 저장한다

`build_track.py` 는 역에서 역까지 선로 그래프로 경로를 찾아 놓고, 그것을
한 줄로 이어 붙이면서 구간 경계를 버리고 있었다. 그리는 쪽은 그 이어붙인
선 위에 역을 다시 투영해 잘랐고, 그 투영이 역 근처에서 모서리를 질러갔다.
京橋·天王寺·新今宮·難波·白鷺 가 전부 그 자리다.

이제 `(노선, 역, 역) -> 좌표열` 로 `raw/track-segments.json` 에 저장하고
그리는 쪽이 그대로 쓴다. 투영도 자르기도 하지 않는다.

- **별도 파일인 이유**: 처음에 `coordinates.json` 에 넣었더니 `build_rail`
  이 같은 파일을 덮어써서, 종별 정규식만 손보고 다시 돌려도 구간 선형이
  오류 없이 사라졌다.
- **열쇠에 노선을 넣은 이유**: 역 쌍만으로 묶으면 같은 역 쌍을 지나는 다른
  노선끼리 덮어쓴다. kanto_osm 에서 그런 역 쌍이 436개다. 日暮里-西日暮里
  를 京浜東北線·山手線·日暮里舎人ライナー 셋이 쓴다.

### 구간끼리 같은 노드를 공유한다

구간마다 역 옆 선로 노드를 따로 골라서, 한 역에서 앞 구간의 끝과 뒤 구간의
시작이 중앙값 166m 어긋났다. 그 어긋남이 화면의 삼각형이다. 앞 구간이 끝난
노드에서 다음 구간을 시작하게 했다. 이음매 2,056개 중 2,010개가 정확히 0m.

### 종착역 앞에서 멈추던 것

`route()` 가 역 반경 400m 안의 후보 중 **그래프 거리가 가장 짧은** 것을
골라서, 선로를 따라가다 반경에 들어서는 순간 멈췄다. 중간역은 티가 안 나고
종착역에서 보인다. 도큐 고도모노쿠니선이 선로가 역까지 0.03m 로 닿아
있는데도 346m 앞에서 끊겼다. 후보는 이미 역에서 가까운 순으로 오므로,
닿는 것 중 역에 가장 가까운 것을 고르게 했다.

    구간 끝과 역 사이   중앙 88m -> 0m,  200m 초과 695개 -> 2개

### 다시 스냅할 때 이어 붙이던 것 (아직 빌드 전)

앞 구간이 끝난 노드에서 길을 못 찾으면 역 옆 후보로 다시 시작하는데, 그때
선을 안 끊고 그대로 이어 붙였다. 스냅 후보가 역에서 400m 까지라 두 자리가
800m 벌어질 수 있다. 銀座線 末広町 186m, 都営浅草線 戸越 173m 가 그것이다.
새 구간이 앞 구간이 끝난 노드에서 시작하지 않으면 끊게 했다.

### 구간 선형 옮기기 (시각표 권역)

`build_geometry.py` 가 OSM 권역의 구간 선형을 ODPT 권역으로 옮긴다.

- 처음엔 0개가 옮겨졌다. `build_track` 에서 역 id 를 담은 변수 `sb` 를 바로
  다음 줄의 기존 코드가 덮어써서 열쇠가 `OSM.0|OSM.0.870|[302276,...]` 꼴로
  저장됐다. 변수 이름을 `tgt` 로 바꿨다.
- 다음엔 1,340개. ODPT 역을 **가장 가까운 OSM 역 행**에 맞췄는데, 같은 자리에
  여러 노선의 역 행이 겹쳐 있어 엉뚱한 노선의 행이 집혔다. 日比谷線 八丁堀 이
  JR京葉線 의 행에 맞춰져 22개 구간 중 6개만 옮겨졌다. 역 행이 아니라 **묶음
  번호**로 맞추게 고쳐 1,993개.
- 아직 빌드 전: 노선 짝을 하나로 정해 두고 그 안에서만 찾던 것을, **역 묶음
  쌍 색인**으로 바꿨다. 総武快速線 은 역 18개가 전부 OSM 에 있는데 구간이
  0개였다. 짝으로 정해진 노선이 그 역 쌍을 인접으로 안 들고 있어서다.

### 구간 선형이 낡은 역 id 를 들고 있었다

芝山千代田 이 지도에서 외딴 점이 됐다. 芝山鉄道線 과 東成田線 둘 다
안 그려져 있었다. 노선도 역도 멀쩡히 있는데 **선형이 없어서** 두 점짜리
직선이 되고, 그 직선이 400m 를 넘어 버려졌다.

`raw/track-segments.json` 은 있었다. 그런데 열쇠에 적힌 역 id 가 지금
`stations.json` 에 없는 것이었다. OSM 은 다시 훑을 때마다 id 를 새로
매기는데, `build_rail` 만 다시 돌리고 `build_track` 을 안 돌려서 구간
선형이 옛 id 를 들고 남아 있었다.

    구간 선형 중 지금 id 와 맞는 것
    간토 1,993/1,993,  간사이 1,927/1,927,  간토OSM 2,261/2,428

간토OSM 만 167개가 낡았다. `build_rail` 뒤에는 반드시 `build_track` 을
돌려야 한다. HANDOVER.md 의 순서가 그렇게 돼 있는데 지켜지지 않았다.

### 역에서 찌르고 돌아오는 모양

`build_track` 은 역 반경 안의 선로 노드 하나를 골라 거기서 거기까지 잇는다.
그 노드가 역 너머에 있으면 선이 역을 지나쳐 갔다가, 다음 구간이 같은
노드에서 시작해 그대로 되돌아온다. 화면에서는 역에서 뾰족하게 찌르고
돌아오는 모양이 된다. 소테츠 계통의 海老名, 긴자선 赤坂見附·溜池山王,
사이쿄선 赤羽 가 그 자리다.

두 가지를 넣었다. 둘 다 그리는 쪽이라 재빌드가 필요 없다.

1. `geometry._trim_overshoot` — 구간 선형을 제 두 역 사이로 자른다.
   앞뒤 꼬리만 자르고 가운데는 안 건드린다. 한쪽이 선형의 4분의 1을
   넘게 잘려 나가면 되돌아온 꼬리가 아니라 역을 잘못 짚은 것이므로
   그대로 둔다.
2. `geometry.unwind_retrace` — 이음매에서 앞 구간의 끝과 뒤 구간의
   시작이 같은 길을 되짚으면 그만큼 양쪽에서 걷어낸다. 두 구간이 역 옆
   같은 노드를 공유하는데 그 노드가 본선에서 벗어나 있을 때다.

재는 잣대는 "30m 넘는 걸음 둘이 120도 넘게 꺾이는 자리" 다.

**세 번째로 넣은 것이 제일 크다.** `geometry.smooth_spikes` 는 그린 선을
훑으면서 갔다가 되돌아왔다 다시 가는 점을 뺀다. 원인은 자리마다 다르지만
(이음매 겹침, 빌려 온 선형이 몇 점 어긋남, 선로 그래프가 회차선을 물고 옴)
모양은 늘 같아서, 그리는 마지막에 한 번 훑는 것이 제일 확실하다.

    간토 그린 선의 되꺾임
    332 -> 251 (앞뒤 꼬리 자르기)
        -> 183 (이음매 되짚기 걷어내기)
        ->  27 (되꺾이는 점 빼기)

**스위치백을 지키는 법.** 진짜로 되돌아가는 선로가 있다(하코네 등산철도의
大平台·出山信号場). 처음에는 "되돌아온 길이가 400 m 를 넘으면 손대지
않는다" 로 막았다. 다음에는 "그 노선의 역 옆 점은 안 지운다" 로 바꿨는데
**더 나빠졌다.** 되꺾임이 대개 역 근처라 정상 제거까지 막힌다.

    한도 200m, 역 판정 있음    82        한도 200m, 역 판정 없음    70
    한도 500m, 역 판정 있음    79        한도 2000m, 역 판정 없음   67

지금은 조건을 좁게 잡는다. 점이 역에 30 m 안으로 붙어 있고, 빼고 나면
선이 그 역에서 200 m 넘게 떨어질 때만 둔다. 그러면 판정 없이 잰 최선과
같은 67개가 나오면서 하코네 등산철도의 스위치백이 살아 있다
(大平台 가 그린 선에서 99 m).

    되꺾임  간토 24,  간토OSM 33,  간사이 10
    야마노테선 0,  긴자선 0,  히비야선 0,  JR닛코선 0,  조반선 0

`build_naive` 가 `ride_path` 로 구간 거리를 재므로 가상 시각표의 소요
시간에도 반영된다. 자르고 나서 다시 돌려야 한다.

### 제 선로를 못 찾던 진짜 원인 둘

구간 선형 코버리지가 간토OSM 94%, 간토 83% 에서 안 올라갔다. 원인이 둘
있었고 둘 다 진짜였다.

**1. 괄호 안에 든 노선 이름을 버리고 있었다.** OSM 은 사이쿄선 선로를
`東北本線（埼京線）` 이라고 적어 둔다. `norm_line` 이 괄호를 통째로
지워 `東北本線` 만 남기니, 埼京線 은 제 선로를 한 구간도 못 찾고 매번
남의 선로로 떨어졌다. 이제 괄호 안도 열쇠로 쓴다(`line_keys`).

**2. 역 좌표를 묶음 하나로 뭉개고 있었다.** build_rail 이 노선별로
승강장 좌표를 갈라 놨는데(간토OSM 804개), build_track 이
`cpos.setdefault(s["cluster"], ...)` 로 묶음당 하나만 남겨 되돌려 썼다.
武蔵浦和 는 무사시노선이 지상, 사이쿄선이 고가라 붙는 선로 후보가 전부
무사시노선이었고, 그래서 사이쿄선이 서쪽으로 332 m 불룩하게 돌아 나왔다.
이제 역 줄마다 제 좌표로 붙인다(`snapped_sid`).

    구간 선형   간토OSM 94.3% -> 99.5% (못 이은 구간 149 -> 12)
                간사이   97.0% -> 99.8% (60 -> 4)
    武蔵浦和 → 中浦和   남의 선로 1,567m(+25%) -> 제 선로 1,219m
    北戸田 → 武蔵浦和   못 이음 -> 제 선로 2,356m

**시각표 권역도 직접 잇는다.** 전에는 OSM 권역에서 뽑아 옮겨오기만 했다.
묶음 번호가 없는 권역은 역 하나를 묶음 하나로 보게 고쳐, ODPT 역 좌표를
OSM 선로에 바로 붙인다. 간토가 93.5% -> 98.8% 가 됐다. coordinates.json
은 ODPT 원본이라 덮으면 안 되므로 `TRACK_ONLY=1` 로 막는다.

    REGION=kanto TRACK_ONLY=1 python src/build_track.py

남의 선로에 벌점(4배)을 주는 것도 같이 넣었지만, 실제 효과는 위 둘이 냈다.

### 되꺾임은 선형이 아니라 단순화가 만들고 있었다

"역에서 Z 자로 꺾인다" 를 오래 쫓았는데, 마지막에 잣대를 의심하고
허용오차를 0 으로 두고 재 보니 이렇게 나왔다.

    허용오차 0.01m (사실상 원본)   되꺾임  4   점 266,728
    허용오차 1m                           10   점 122,112
    허용오차 3m (그동안 쓰던 값)             26   점  69,230
    허용오차 8m                           85   점  40,505

**선로에 120도 넘는 자리는 세 권역 통틀어 4곳뿐이다.** 나머지는 반경이
작은 곡선을 3 m 로 줄이면서 부드러운 곡선이 한 꼭짓점의 날카로운 각이
된 것이다. 선형은 멀쩡한데 화면에서만 꺾여 보였다.

허용오차를 통째로 낮추면 점이 76% 는다. 그래서 `_simplify` 가 줄인 뒤
**꺾여 보이는 자리만 원본을 되살린다**(100도 넘으면 그 구간을 8분의 1
허용오차로 다시 줄인다). 점은 205개만 늘고(69,230 -> 69,435) 되꺾임이
26 -> 10 이 됐다.

    되꺾임  간토 6,  간토OSM 2,  간사이 2

**재는 자를 의심하는 데 너무 오래 걸렸다.** 중간에 "선이 역에서 837m
떨어졌다" 고 보고한 것도 잘못이었다. 선까지의 거리가 아니라 가장 가까운
꼭짓점까지를 쟀는데, 단순화로 꼭짓점이 솎이면 선은 역을 지나도 꼭짓점은
멀다. 제대로 재면 역이 선에서 200 m 넘게 떨어진 자리는 7,285개 중 22개다.

### 노선 쪼개기 (넣었다가 걷어냄)

OSM 은 운행 계통대로 관계를 만든다. `阪急京都本線・千里線 普通
(大阪梅田 => 北千里)` 는 大阪梅田-淡路 를 교토 본선 선로로 달리고
淡路-北千里 만 센리선이다. 그래서 센리선이 3역짜리 토막으로 남는다.

역 목록으로 갈라 두 노선의 조각으로 미는 것을 만들었다. 이름이 가리키는
노선일 때만 쪼개야 한다 — 아무 노선이나 고르면 埼京線・川越線 의
大崎-池袋 가 야마노테 화물선을 달린다는 이유로 "야마노테선" 이 되어
사이쿄선이 통째로 사라졌다.

**쓰지 않기로 했다.** 센리선과 교토 본선은 열차가 따로 오는 것이 아니라
같은 열차가 淡路 에서 직통하는 것이라, 갈라 놓는 것이 실제 운행과 더
멀어진다.

### 남의 선로를 타는 문제 (아직 안 끝남)

구간 선형이 있는 자리는 제 선로를 탄다. 없는 자리는 예전 방식으로 떨어지고,
그건 **근처를 지나는 아무 노선의 선형이나 골라 쓰는 것**이다.

    구간 선형이 있는 비율   간토 98.8%,  간토OSM 99.5%,  간사이 99.8%
    (위 "제 선로를 못 찾던 진짜 원인 둘" 을 고친 뒤)

秋葉原·神田 에서 노선들이 서로의 선로를 타고, 신주쿠에서 게이오선이 488m
벗어나고, 大崎 발 武蔵野線 이 사실은 埼京線 인 것이 전부 이 자리다.

남의 선형을 아예 못 쓰게 막는 것은 **틀린 답**이다. 직통운전은 실제로 남의
선로를 달린다. 코버리지를 올리는 쪽으로 가야 한다. 역 쌍 색인이 그 첫
걸음이고, 그래도 안 채워지는 것이 있다.

    伊東線·駿豆線·伊豆急行線·富士急行線·東海道線
    -> 역이 OSM 권역 안에 0개다. 시즈오카·야마나시라 범위 밖이다.
       권역을 넓히지 않는 한 방법이 없다.

---

## 역

### Z 자 꺾임 = 왕복 중복

관계가 갔다 오는 순서를 그대로 들고 있어서 선이 갔다가 그대로 돌아왔다.
`repair_order` 가 중복을 지운 목록을 만들어 놓고 `return seq` 로 버리고
있었고, 비교도 중복 있는 원본과 중복 없는 수정본 사이에서 해서 올바른
수정이 불리했다.

    간사이 38/146 -> 1개,  간토OSM 28/174 -> 1개
    京浜東北線 73역 -> 47역 (ODPT 와 같아짐)

짧은 노선(`len(plain) < 4`)이 빠져나가던 구멍과, 순환선의 닫는 역까지
지우던 것을 막았다. 순환선은 첫 역과 끝 역이 같고 나머지가 전부 다를 때
손대지 않는다.

### 빠진 역 메우기 (`fill_missing`)

OSM 이 어떤 역에는 선로 위 정차 노드를 안 두고 옆에 역 노드만 둔다. 그러면
웨이 구성 노드만 보는 방식으로는 통째로 빠진다. 거르는 것이 넷이다.

1. 후보는 `railway` 가 역인 노드 중 어느 계통도 정차역으로 안 부른 것만.
   없으면 나란히 달리는 남의 노선 역이 온다(山陰本線 120m 안에 사가노
   관광철도 トロッコ嵯峨).
2. 이름이 이미 있는 역과 같으면 넘긴다 (浜坂 / 浜坂駅).
3. `FILL_SAME_M = 200` — 이미 정차역이 있는 자리면 넘긴다. 이름 없는 정차
   노드는 이름으로 못 가려서 자리로 가린다.
4. `usage` 가 training·military·test·industrial·tourism 이면 뺀다. 도쿄에
   JR동일본 승무원 훈련용 모의역(志茂田·大田)이 railway=station 으로 있어서
   京浜東北線 川崎-蒲田 사이에 끼어들었다.

**사슬마다 따로** 세우고, 이미 있는 역 둘 사이에 든 것을 끼운다. 사슬을
이어 붙인 거리로 한 줄에 세우면 안 된다. `stitch_all` 이 주는 사슬은 순서도
방향도 노선을 안 따라서, 제자리를 못 찾은 역이 목록 맨 앞뒤로 간다.
和歌山線 머리의 薬水, 泉北線 꼬리의 なんば 가 그랬다.

### 정차 순서를 길이로 바로잡는다

목록 끝에 엉뚱하게 붙은 역이 남아 있었다. 常磐線 의 偕楽園 이 45개 중
맨 끝에 붙어 大津港 에서 60 km 가 됐고, 山陰本線 의 玄武洞 이 꼬리에
37 km 로 붙었다. 지도에는 조각이 끊겨 안 그려지지만 **경로 그래프에는
간선으로 남아** 60 km 를 한 정거장으로 건너뛴다. 도달권역이 거짓으로
부푼다.

`repair_order` 가 이상치에서 끊기는 하는데, `join_runs` 는 블록을
끝끼리만 이어서 한 역짜리 블록을 가운데로 넣지 못한다.

더 나쁜 것은 순서가 통째로 뒤섞인 관계다. 成田線(成田空港→千葉)은
千葉 이 가운데 끼어 11 km 를 왕복하고, 四街道 와 都賀 가 뒤집혀 있고,
成田 이 맨 끝에 가 있었다. 이건 이상치 하나를 옮겨서는 못 고친다.

`reseat_strays` 는 **노선은 길이라, 역을 순서대로 이은 길이가 짧을수록
옳은 순서다** 로 간다. 한 역을 빼서 가장 덜 돌아가는 자리에 다시 넣고,
전체 길이가 1% 넘게 줄 때만 받는다. 줄지 않으면 그대로 둔다. 버리지는
않는다. 자리만 틀렸을 뿐 진짜 역이다. 순환선은 첫 역과 끝 역이 같아
길이로 재면 안 되므로 건드리지 않는다.

    순서가 바뀐 노선  간토OSM 20개, 간사이 21개.  길어진 것 0개. 0.1초.

    JR成田線 (成田空港 → 千葉)   52.5 ->  33.9 km
      전  東千葉 四街道 都賀 千葉 物井 佐倉 酒々井 空港第2ビル 成田空港 成田
      후  千葉 東千葉 都賀 四街道 物井 佐倉 酒々井 成田 空港第2ビル 成田空港
    紀勢本線        445.2 -> 332.2 km      山陽本線 (A)  144.8 -> 86.0 km
    総武本線        119.0 ->  78.4 km      近江鉄道本線   81.5 -> 46.4 km
    常磐線 최대 이웃간격 60.1 km -> 7.2 km (偕楽園 이 赤塚 와 水戸 사이로)

**버리는 쪽으로 먼저 만들었다가 걷어냈다.** 끼워 넣은 역 중 간격을 크게
벌리는 것을 빼는 `_drop_far_fills` 를 넣었더니 偕楽園 은 관계가 직접
들고 있는 역이라 안 잡혔고, 대신 玄武洞 처럼 자리만 틀렸을 뿐 진짜인
역을 없앴다. 그 노선만 서는 역이면 권역에서 통째로 사라진다.

**전에는**: 정차역이 2개 이상인 관계는 이 코드를 아예 안 거쳤다.
"되살릴 필요가 없다" 와 "빠진 역을 메울 필요가 없다" 를 한 덩어리로 묶어
놨다. JR日光線 이 정차역 6개를 들고 있어서 건너뛰었고, 그래서 日光 역이
끝내 안 들어왔다(후보에도 있고 선로에서 92m 인데도). 이제 모든 여객 노선에
대해 메운다.

### 승강장이 다른 같은 이름 역

묶음의 첫 노드 좌표를 그 묶음을 지나는 모든 노선이 공유했다. 豊島園 은
세이부와 오에도가 111m 떨어져 있는데 세이부선이 오에도 승강장으로 끌려갔다.

거리로는 못 가린다. 이름이 같은 역 노드 쌍을 거리대별로 세면 100-150m 에
中野坂上·九段下(한 역)와 豊島園(다른 역)이 같이 있다.

그래서 **묶음은 그대로 두고 좌표만 가른다**. 노선마다 제 노드를 쓰고,
검색·세는 단위는 묶음 그대로다. 지도에는 `spots` 로 승강장마다 찍는다.

    간토OSM 804개, 간사이 469개 역 줄이 제 승강장 자리로 갈라진다 (빌드 전)

### 걸어서 갈아타는 이웃 역

高槻市(한큐)와 高槻(JR)는 520m 떨어져 있고 사람들은 걸어서 갈아탄다. 구글
지도도 그 환승을 내놓는다. 우리 기준이 400m 라 그 쌍이 빠져 있었고, 라우터는
한참 돌아가는 길을 골랐다. 시각표가 아니라 환승망이 없어서 생긴 일이다.

더 나빴던 건 간토다. 시각표 권역은 거리 기반 규칙이 아예 없고 ODPT 역 그룹
파일만 썼다. 그 파일은 같은 역 구내만 묶는다.

    못 잇고 있던 1km 안 역 쌍
    간토 3,122쌍, 간토OSM 1,377쌍, 간사이 1,194쌍

`src/transfers.py` 를 새로 만들어 두 빌더가 같은 규칙을 쓰게 했다. 범위를
1km 로 넓히되 값을 거리에서 매긴다.

    비용 = max(300초, 90초 + 거리 x 1.25 / 도보속도)
    520m -> 9.6분 (구글과 비슷),  1km -> 17.1분

예전처럼 400m 안이면 전부 300초로 두고 범위만 넓히면 1km 를 5분에 걷는
셈이 되어 도달권역이 거짓으로 부풀어 오른다.

---

## 이름·번역

### 노선 이름 사전 `data/line-names.json`

466개 노선을 일본어 이름을 열쇠로 언어별로 적어 둔다. 손으로 고치면
서버만 다시 올리면 반영된다. **이 파일이 이긴다.**

    노선 이름 한국어   간토 100%, 간토OSM 100%, 간사이 100%

- OSM 관계에는 `name:ko` 가 19%(간토OSM), 10%(간사이) 뿐이다. 영어는 98%.
- ODPT 는 5개 언어를 다 갖추고 있어서, 역 목록이 겹치는 노선끼리 짝지어
  빌려온다. 다만 **역 목록이 합집합 대비 80% 넘게 겹칠 때만** 빌려준다.
  느슨하게 했더니 きぬがわ列車 와 東武日光線 이 JR日光線 의 이름을 받아
  "닛코선" 이 됐다.
- 빌려올 짝이 없는 것은 손으로 적었다. 간토 20개, 간사이 131개.
  빌려오기를 80% 로 조인 뒤 간토OSM 18개가 이름을 잃어 거기도 적었다
  (사이쿄선, 무사시노선, 쓰루미선, 히가시나리타선, 가와고에선,
  소테쓰·JR 직통선, 쇼난신주쿠라인, 이즈미노선, 하코네 등산철도선,
  세이부치치부선 등).

**회사 이름이 노선 이름의 일부인 것.** `_lead` 가 앞을 떼면 이름이
망가지는 것들이 있다. 사전 항목에 `"keep": true` 를 달면 떼기에서 뺀다.
처음에는 사전에서 온 이름을 전부 건너뛰게 했는데, 그러면 117개 항목에
적힌 "JR 가코가와선" 같은 것까지 살아나 JR 떼기가 도로 무너진다.

    京王線      "선"      -> 게이오선
    京王新線     "신선"     -> 게이오신선
    西武園線     "엔선"     -> 세이부엔선
    動物園線     "쓰엔선"    -> 도부쓰엔선   (どうぶつえん 을 "도부"+"쓰엔" 으로 읽었다)
    西武秩父線             -> 세이부치치부선 (秩父鉄道 의 지치부 본선과 갈린다)

京王線 이 "선" 한 글자로 나오고 있었다.

### 역 이름 사전 `data/station-names.json`

2,023개. 같은 방식이다. ODPT 가 갖고 있는 이름을 모아 두고 비어 있는
언어만 채운다.

    역 이름 한국어   간토 100%, 간토OSM 100%, 간사이 100%

간사이는 빌려올 시각표 권역이 없어 76% 에서 멈춰 있었다. OSM 이 영어
(헵번 로마자)를 갖고 있는 역이 많아서, 로마자를 외래어 표기법으로 옮기는
변환기를 짜서 채웠다. 중요한 것은 **검증할 수 있었다는 점**이다. 이미
한국어가 있는 1,241쌍에 돌려 보고 틀린 것을 보며 규칙을 고쳤다.

    88.9%  첫 판
    91.7%  ん 을 m 으로 적는 헵번 처리 (Tambaguchi, Temmabashi)
    92.7%  장음은 마크롱만 믿는다 (豊岡 도요오카 / 勝浦 가쓰우라 를 살리려고)
    92.9%  훈령식(ti·tu·si)과 외래음(fa·che·je)

안 맞는 나머지는 대개 원본 쪽 오류였다(安堂 에 "가타시모" 가 적혀 있고,
Takayau 처럼 로마자에 오타가 있다).

로마자로 만든 327개 중 12개(가타카나 외래어 アイランドセンター 등)를
손보고, 로마자가 아예 없는 88개(六十谷 무소타, 膳所 제제, 百舌鳥 모즈)는
손으로 적었다.

### 표기 통일

- 이름 앞의 회사 이름을 뗀다. 목록이 회사로 묶어 보여주므로 겹쳐 적을
  이유가 없다. ODPT 는 山手線 을 "야마노테선", OSM 은 "JR 야마노테선" 으로
  적어 두는 식이라 섞이면 지저분하다.
- **이름이 겹치는 노선에만** 회사를 다시 붙인다. 다만 **JR 에는 안 붙인다.**
  JR 노선이 압도적으로 많아 다 붙이면 목록이 회사 이름으로 도배된다.
  겹치는 쪽만 붙여도 갈린다. 日光線 은 `닛코선`(JR) / `도부 닛코선` 이다.
  JR 회사끼리 겹칠 때만(東海道本線 이 JR동일본과 JR도카이에 다 있다) JR
  쪽에도 붙인다.
- 겹치는지는 **꼬리표를 뗀 이름**으로 본다. 西武新宿線 은 "신주쿠선",
  都営新宿線 은 "신주쿠선 (신주쿠 → 모토야와타)" 라 글자로는 달라서
  한 무리로 안 잡혔고, 그래서 둘 다 회사 없이 "신주쿠선" 으로 보였다.
- "게이요 선" 처럼 선 앞이 띄어져 있던 것을 "게이요선" 으로 맞춘다.
  "산요 본선", "아비코 지선" 은 그대로 둔다.
- 都営 노선이 浅草線 은 "도에이 아사쿠사선", 三田線 은 "미타선" 으로
  적혀 있던 것을 손으로 맞췄다.

### 운영사

OSM 의 `operator` 태그는 회사 이름이 아닐 때가 있다. 日比谷線 은
`東京メトロ日比谷線;東武スカイツリーライン` 이라 아는 회사가 하나도 없는
것으로 읽혀 "그 밖" 으로 떨어졌다. 문자열 안에 아는 회사 이름이 들어 있으면
그걸 회사로 본다.

태그가 아예 없는 노선은 두 단계로 메운다.

1. 이름이 JR 로 시작하면 그 권역에서 가장 흔한 JR 회사.
2. 그래도 비면 역을 절반 넘게 공유하는 노선의 회사를 물려받는다.

    운영사 없는 노선   간토OSM 28개 -> 13개, 간사이 -> 6개

**회사 이름을 한 표에서만 가져온다.** 표가 둘이었다. 시각표 권역은 노선 id
앞머리로 찾는 `OPERATORS`(일본어·한국어뿐), OSM 권역은 `operator` 태그로 찾는
`RAIL_OPERATORS`(5개 언어). 같은 회사가 권역마다 다른 이름으로 불려서
디버그 패널의 무리가 갈렸다.

    전   시각표판 "도에이"      OSM판 "도영지하철"
    후   양쪽 다 "도영지하철" (ja 都営地下鉄, en Toei Subway)

`PREFIX_COMPANY` 가 id 앞머리를 회사 표에 잇고, `title()` 이 회사 표를
먼저 본다. 간토 46개 회사가 전부 5개 언어를 갖췄다. 없던 회사 넷을
더했다(芝山鉄道·伊豆急行·富士急行·会津鉄道). 伊豆急行 "이즈큐행" ->
"이즈급행", 富士急行 "후지큐행" -> "후지급행" 도 고쳤다.

`src/operators.py` 에 회사 이름 표(5개 언어)와 표기 흔들림 별칭을 뒀다.
相模鉄道 / 相鉄 / 相模鉄道株式会社 가 한 회사다.

그래도 남는 것이 간토OSM 13개, 간사이 6개였다. 세 가지를 더했다.

1. **노선 이름 안에서 회사를 찾는다.** OSM 은 태그를 비워 두고 이름에만
   적어 두는 일이 많다(真岡鐵道真岡線, 上毛電気鉄道上毛線, 西武鉄道狭山線).
   `_canon_operator` 는 못 찾으면 적힌 문자열을 그대로 돌려주므로 여기엔
   못 쓴다. 찾은 것만 돌려주는 `operator_in_name` 을 따로 뒀다.
2. **줄여 적은 회사 이름을 별칭에 넣었다.** 東武東上線 에 "東武鉄道" 는
   없고 "東武" 만 있다. 東武·西武·京成·京王·小田急·東急·京急·阪急·阪神·
   京阪·南海·近鉄 을 넣었다.
3. **회사 표에 없던 회사를 더했다.** 真岡鐵道, 上毛電気鉄道, 小湊鐵道,
   流鉄, 鹿島臨海鉄道, 宇都宮ライトレール, 能勢電鉄, 智頭急行,
   北大阪急行電鉄, 泉北高速鉄道.

그래도 이름에도 회사가 없고 JR 로 시작하지도 않는 것이 남는다(両毛線).
`data/line-operators.json` 에 손으로 적는다. 권역으로 나눈 것은 東海道本線
이 간토에서는 JR동일본, 간사이에서는 JR서일본이기 때문이다.

    운영사 없는 노선   간토OSM 13개 -> 0개, 간사이 6개 -> 0개

京成松戸線 은 北総鉄道 가 아니라 京成電鉄 다. 2025년에 新京成電鉄 가
京成電鉄 에 합병되면서 新京成線 이 이 이름으로 바뀌었다.

---

## 노선 디버그 패널

지도 오른쪽 위 "노선 디버그" 버튼.

- 운영사로 묶고 기본은 접는다. 노선이 하나뿐인 회사는 "그 밖" 으로 모으고
  이름 옆에 회사를 작게 적는다.
- 모두 켜기 / 모두 끄기(지도에 그릴지), 모두 펼치기 / 모두 접기(목록을
  펴고 접을지). 하는 일이 달라서 줄을 나눴다.
- 조각난 노선은 **지선 이름 (기점–종점)** 양식으로 편다. 지선 이름이
  없을 때만 `(기점–종점)` 으로 구간만 적는다. 조각마다 id 가 달라서
  개별로 끌 수 있다.
- 지선은 본선 밑으로 접어 넣는다. 이름이 완전히 같은 노선도 한 줄로 묶고
  구간으로 편다(武蔵野線 이 간토에 6개다).
- 이름 끝의 `(출발 => 도착)` 꼬리표는 뗀다. 떼면 이름이 겹치는 것은 그대로
  둔다.
- 지도에서 선 위에 1초 머물면 이름이 뜨고(체크박스로 켠다), 누르면 그
  회사를 펴고 그 줄까지 스크롤한다.
- 지도를 눌러도 안 닫힌다. 언어를 바꾸면 바로 다시 그린다. 폭은 고정이다.

**검색창**은 줄을 한 번만 만들고 보이기/숨기기만 한다. 예전에는 한 글자
칠 때마다 메뉴를 통째로 다시 그려서 입력칸이 DOM 에서 떨어져 나갔고, 한
글자만 입력됐다. 거기에 포커스를 되돌리는 코드를 덧붙였던 것을 걷어냈다.
증상을 덮은 것이라 잘못됐다.

**애칭이 붙은 열차 계통은 지도에 안 그린다.** えのしま·ひたち·特急はるか
같은 것은 노선이 아니라 그 선로를 달리는 열차다. 제 역이 다른 노선에 95%
이상 들어 있을 때만 뺀다. 시각표에는 그대로 남아 경로 계산은 안 바뀐다.

**종별만 적힌 것도 같이 뺀다.** 相鉄直通線, 小田急通勤急行,
快速急行 (神戸三宮 => 近鉄奈良), 東京地下鉄の直通運転 - 常磐線 이 노선인
척 목록에 서 있었다. 회사 이름을 떼고 남은 것이 종별 한 마디뿐이거나
이름에 直通運転 이 들어 있으면 계통으로 본다. 회사 이름 떼기는
`operators.strip_operator_head` 다. 이것도 95% 규칙을 똑같이 거치므로,
제 역이 있는 계통은 안 사라진다.

北大阪急行電鉄南北線 이나 智頭急行智頭線 처럼 회사 이름에 急行 이 든
노선은 회사를 먼저 떼므로 안 걸린다.

두 번 헛짚었다.

- 꼬리가 종별인 것까지 계통으로 몰았더니 `JR京葉線 各駅停車 (東京 → 蘇我)`
  같은, 그 권역에서 유일한 노선 항목까지 후보가 됐다. 꼬리 규칙은 뺐다.
- 열차 이름 정규식이 `^(…|ひたち|…)` 이라 앞머리만 맞으면 걸렸다.
  `ひたちなか海浜鉄道湊線` 이 "ひたち" 로 잡혔다. 뒤에 이름 글자가 오면
  안 되게 막았다. `つくばエクスプレス` 도 `エクスプレス` 한 단어에 걸려
  있었다. 진짜 노선이라 따로 뺐다.

---

## 그 밖

- `수도권 도달권역` 옆의 "Greater Tokyo · Transit Isochrone" 도 언어별로 바꿨다.
- 지도 바꾸기 버튼이 글이 흐르는 문단 안에 있어서, 위의 `map-note` 길이가
  달라질 때마다 밀렸다. 문단 밖 제 줄로 빼고 바뀌는 글을 아래로 옮겼다.
- 검색 목록에서 東京 이 두 줄로 나오던 것. 이름이 같고 700m 안이면 검색에서는
  한 줄로 합친다(도쿄역은 게이요선 승강장이 본 개찰에서 500m 떨어져 있다).
  지도에는 승강장마다 따로 찍는다.
- 검색 목록의 운영사가 `OSM` 으로 나오던 것. 역 id 접두사가 아니라 그 역이
  속한 노선의 operator 를 쓴다.
- 디즈니 리조트 라인을 뺐다(`data/excluded-lines.json`). 역내 운송수단이다.

### 보안 점검에서 고친 것

- `src/fetch_data.py` 가 원격이 정한 파일 이름을 그대로 경로에 붙이고 있었다.
  GitHub API 응답에 `../` 가 오면 받을 폴더 밖에 쓴다. 한 칸짜리 이름만 받는다.
- `thresholds` 에 개수 제한이 없었다. 값마다 등시간선을 따로 뽑으므로
  수만 개를 보내면 그만큼 일한다. 12개로 막았다.
- 서버가 권역 하나를 못 올리면 통째로 안 뜨던 것. 못 올린 권역만 뺀다.

---

## 도카이 (2026-09-21)

아이치·기후·시즈오카·미에. 격자 322×326km, 추출본은 주부·간사이(미에)·
간토(아타미 건너편 사가미만 기슭). 격자 안 육지 67.3%(간사이 56.8%).

    노선 72  역 1,220  역 묶음 1,091  지원 역 1,088
    구간 선형 99.8%   통과 계통 48 (위키 표 14개에서 39)
    노선·역 이름 한국어 100%, 운영사 없음 0

만들면서 고친 것.

- **踊り子·ひだ 가 노선으로 섰다.** 特急 이 이름에 없어서다. 伊東線 이 3역,
  高山本線 에 특급 정차역 7개가 빠져 있었다. 특급 애칭 목록을 두고, 빠진 역
  후보에서 특급이 부른 역은 거르지 않는다. 후보 거름을 "각역정차가 아닌 것
  모두" 로 풀었다가 간토OSM 시뮬레이션에서 新日本橋 가 銀座線 에 붙어 특급으로
  좁혔다.
- **樽見鉄道 가 통째로 없었다.** 선로 관계만 있고 역이 선로 옆에 있다.
  선로 관계도 후보 역에 한해 선로 옆에서 줍는다. 19역이 다 들어왔다.
- **名鉄犬山線 이 본선과 안 만났다.** `line-extensions.json` 으로 名鉄名古屋
  까지 잇는다. 나고야-이누야마 80분 -> 52분.
- **역에 현이 하나도 안 붙었다.** `build_admin` 을 `build_naive` 앞에서 돌려
  옛(임시) 역 목록에 붙었다. 순서를 바꿨다.
- **RAIL_REUSE 가 처음 돌자 멈췄다.** 캐시에서 읽은 선로 좌표가 numpy 배열인데
  `if geom.get(w)` 로 따졌다.
- **JR伊東線 이 JR도카이로 나왔다.** 손으로 적은 회사가 JR 짐작보다 뒤였다.
- **ロムニー鉄道**(슈젠지 테마파크 안 소형 열차)를 제외 목록에 넣었다.
- 회사 15곳을 `RAIL_OPERATORS` 에, 노선 이름 48개와 역 이름 134개를 사전에
  넣었다. 御殿場線 을 "고템바" 에서 "고텐바" 로 고쳤다(ODPT 표기와 같다).
- 회사 떼기가 "욧카이치 아스나로철도" 처럼 띄어 적힌 회사를 못 뗐다. 고치니
  네 권역에서 13개 노선이 바뀌었는데 지바 모노레일이 "1호선" 만 남아, 번호만
  남으면 되돌리게 했다.

사용자가 지도를 보며 짚은 것(2026-09-22).

- **新堂·佐那具 가 미지원.** 関西本線 선로 관계가 사슬 191개로 쪼개져 이가 쪽
  역이 버려졌다. `fill_missing` 을 사슬 대신 "가장 덜 늘어나는 자리" 로 바꿨다.
- **近鉄名古屋線 이 桃園-伊勢中川 에서 끊김.** 왕복 펴기가 돌아오는 반에만 있는
  역 20여 개를 伊勢中川 뒤로 거꾸로 붙였다. 132.6km -> 76.7km(실제 78.8km).
- **운도코엔마에 미지원.** 東田本線 의 한 정거장짜리 지선이었다. 묶인 계통 끝의
  역을 지선으로 세운다.
- **에쓰미난선이 前平公園-美濃太田 사이 끊김.** 分岐駅 美濃太田 를 사전으로 잇는다.
- **요로선이 大垣 에 안 닿음.** 스위치백을 이음매 되짚기로 걷어냈다.
  `switchbacks.json` 에 적어 건너뛴다.
- **히가시야마선이 名古屋 에서 名鉄 선로를 탐** — 아니었다. OSM 東山線 웨이 위에
  0m 로 놓여 있고, 名鉄 名古屋本線 이 48~110m 옆 지하로 나란히 간다.
- **가미이이다선 2역** — 실제로 上飯田-平安通 0.8km 다. 名鉄小牧線 을 平安通 까지
  잇는 사전 한 줄을 넣었다.
- 규칙을 넓힐 때마다 간사이·간토OSM 을 HEAD 코드와 나란히 돌려 견줬다. 이 과정에서
  세 번 되돌렸다. 묶인 계통의 역을 한가운데까지 합치기(西舞鶴 가 山陰本線 에),
  떠돌이 정리를 복선 목록에 통째로 적용하기(東海道本線 310km -> 575km), 다른 계통이
  부르는 역까지 노선 끝 후보로 넓히기(버스 정류장 노드가 줄줄이).

## 규슈 (2026-09-22)

규슈 7현과 야마구치. 추출본은 규슈·주고쿠. 노선 66, 역 1,341, 역 묶음 1,130.
처음에 `build_rail` 을 `build_admin` 보다 먼저 돌려 현 경계 없이 격자로 걸렀다.
처음 만드는 권역은 인수인계의 순서(보행망·행정경계 먼저)를 그대로 따라야 한다.

사전에 넣은 것.

- 회사 17곳(JR규슈, 니시테쓰, 후쿠오카 시영지하철, 기타큐슈 모노레일, 시전 셋 …).
  `operator` 태그에 `JR Kyushu`, `Kitakyushu Urban Monorail` 처럼 영어로 적힌 것이
  있어 별칭을 달았다. JR山口線 은 태그가 없어 `line-operators.json` 에 JR서일본.
- 특급 애칭(ソニック, にちりん, ゆふ, きらめき, 有明 …). 「にちりん」 처럼 낫표로
  싸 적은 것과 スーパーおき1号 처럼 호수가 붙은 것도 잡게 했다.
- 노선 한국어 이름 62개, 역 194개(변환기 + 손).
- 제외: 志布志線(폐선), 安房森林軌道, 門司港レトロ観光線.

모든 권역에 걸리는 것으로 고친 것.

- **name:ko 꼬리.** "하카타 역", "사카에마치 역", "도스 시". `clean_station_name` 이
  한글 뒤의 "역" 과 띄어 쓴 "시" 를 뗀다.
- **캐시로 돌리면 방면만 적힌 승강장이 저 자신에게서 빈 이름을 받았다.** 이름을
  다시 다듬은 뒤 빈 것을 역 목록에서 빼지 않아서다. 간사이 叡山本線 의 茶山 이
  빈 이름이 됐다.
- **부역명.** 茶山 승강장과 茶山・京都芸術大学 역 노드가 한 역으로 안 묶여
  반대 방향 계통이 3역짜리 노선으로 따로 섰다. 이웃한 두 묶음은 "・" 앞까지
  같아도 한 역으로 본다.
- **선로 관계가 빠뜨린 역.** 日豊本線 의 선로 관계 웨이에 都城·五十市 등 6역의
  노드가 없었다. 완행 계통이 뼈대의 이웃한 두 역 사이에 역을 더 적어 두었으면
  끼운다(`_bridge`). 세 번 좁혔다. 모든 뼈대에 쓰면 계통 관계의 틀린 정차
  (JR宝塚線 塚本)가, 선로 위 역 조건을 걸면 오히려 맞는 것(瓢箪山·岐南)이 빠지고
  틀린 것이 남았다. 선로 관계가 뼈대일 때만, 계통이 OSM 에 직접 적은 정차역만
  옮긴다. 같이 들어온 것: 筑肥線 鬼塚, 鹿児島本線 湯之元, 名鉄名古屋本線 岐南,
  近鉄奈良線 瓢箪山.
- **편도 계통 끝에 출발역이 또 적힌 것.** 西鉄天神大牟田線(… 大牟田 西鉄福岡),
  福北ゆたか線, 西鉄貝塚線, 平成筑豊鉄道糸田線. 순환선으로 읽어 62km 를 되돌아가는
  선을 그었다. 닫는 간격만 그 노선의 보통 간격보다 튀면 뗀다(`_false_close`).
- **두 토막을 담은 선로 관계.** 鹿児島本線 이 門司港-八代 와 川内-鹿児島 를 한
  관계에 담아 川内-八代 82km 를 JR 완행이 한 번에 달렸다. 선로 관계가 뼈대인 완행
  노선을 40km 넘는 간격에서 가른다(`split_far`). 계통 관계에 걸면 애칭 목록에
  없는 특급(ひたち, スーパーはこね)이 잘려서 선로 관계로 좁혔다.

- **長崎電気軌道 3号系統 의 桜町 가 없었다.** 2号系統 과 23/24 가 겹쳐 한 노선으로
  묶이며, 다른 선로로 도는 가운데 토막(市役所-桜町-長崎駅前)이 버려졌다. 묶인
  계통이 가운데에서 벗어나는 토막도, 그 역들을 계통이 직접 적었으면 지선으로
  세운다. 뼈대 역에서 100m 안의 역은 표기만 다른 같은 역으로 보고 넘긴다
  (伊太祈曽/伊太祁曽, 北茅ヶ崎/北茅ケ崎, 柴原/柴原阪大前 가 8-11m 라 이 조건 없이는
  3역짜리 지선이 셋 섰다). `_bridge` 도 계통 관계 뼈대로 넓혔다(N'EX 国分寺,
  湘南新宿ライン さいたま新都心·倉賀野).
- **長崎本線 이 東園-肥前古賀 사이에서 끊겼다.** 선로 관계가 浦上-喜々津 의 신선
  (現川 경유)과 구선(長与 경유)을 함께 담아, 구선을 따라가다 신선으로 건너뛰었다.
  어느 갈래가 본선인지 데이터로 못 가려 `line-extensions.json` 의 "빼는 역" 으로
  구선 7역을 뺐다. 구선 역은 JR大村線 普通 이 모두 들고 있다.
- **긴 노선의 먼 끝은 아침에 열차가 없었다.** `build_naive` 가 첫 차를 출발역에서
  05:00 에 띄워서다. 宮崎-都城 271분 -> 75분. 운행 시간만큼 앞당겨 깐다.

규칙마다 네 권역을 같은 캐시로 다시 돌려 노선별 정차 목록을 견줬다. 적은 것
말고는 바뀐 것이 없다(간토OSM 은 바이트까지 같다).

## 주고쿠·시코쿠·도호쿠 (2026-09-22)

주고쿠는 5현(야마구치 포함, 규슈 권역과 겹친다), 시코쿠는 4현, 도호쿠는 남(미야기·
야마가타·후쿠시마)과 북(아오모리·이와테·아키타) 두 권역으로 나눴다. 추출본은
chugoku(+kansai), shikoku, tohoku.

    주고쿠   노선 45  역 874  구간 선형 99.9%
    시코쿠   노선 26  역 520  구간 선형 99.6%
    남도호쿠 노선 25  역 528  구간 선형 99.8%

고친 것.

- **회사 이름의 急行.** 智頭急行智頭線 이 급행 계통으로 몰려 선로에서 역을 되살리지
  못하고 3역만 남았다. 종별을 보기 전에 회사 표의 회사 이름(智頭急行·伊豆急行·
  富士急行)을 지운다.
- **두 갈래 선로.** 予讃線 은 向井原-伊予大洲 가 내륙(内子 경유)과 해안(伊予長浜
  경유)으로 갈리는데 해안 쪽 계통 관계가 없다. `line-extensions.json` 에 "갈래" 를
  두어 뺀 쪽을 적은 차례대로 따로 세운다.
- **노선 끝.** 津山線 의 선로 관계에 岡山 이 없어 "法界院 뒤로 岡山" 을 적었다.
- **"列車" 라는 이름뿐인 계통.** 시코쿠의 列車 (宿毛=>高知) 가 土讃線 과 겹치며
  土佐くろしお鉄道 中村線·宿毛線 을 삼켰다. 제외 목록의 "이름뿐인 계통" 으로 뺐다.
- **폐선·운휴.** スカイレール(2024 폐선), JR美祢線(2023 수해 이후 버스 대체),
  十和田観光電鉄, 옛 井笠鉄道·倉吉線·安芸線·塩江線 선로, 臨海 화물선 둘.
- **북도호쿠 육지가 1% 로 나왔다.** 陸奥湾 을 가리킨 바다 시드가 夏泊半島 위였다.
  육지가 5% 아래면 build_walk 가 경고한다.
- **위키 표.** 주고쿠·시코쿠·도호쿠의 JR 문서는 駅一覧 표 모양이 달라 거의 못 읽는다
  (주고쿠는 一畑電車 하나). 통과 계통이 OSM 관계에 있는 것뿐이다.

### 운행 횟수 데이터 (시험, 기본값 아님)

全国鉄道運行本数データ(https://gtfs-gis.jp/railway_honsu/, CC BY 4.0/ODbL)를
받아 `src/honsu.py` 로 나이브 시각표의 구간 배차에 쓰는 것을 만들었다.
`NAIVE_HONSU=1` 일 때만 쓴다. 선로 기준으로 합친 파일(kukan)을 써야 한다. 노선
기준 파일(rosen_kukan)은 呉線 열차를 山陽本線 선로 위에서도 呉線 으로만 세어
広島-海田市 가 절반으로 읽혔다. 특급은 세지 않은 데이터라 특급 몫은 빼지 않는다.
회사 이름은 "JR西日本" 처럼 줄여 적혀 있어 별칭을 달았다.

    권역     규칙   운행 횟수   (모델/실제 소요 시간 중앙값)
    간토     0.98   1.00        실제 시각표 275쌍. 사분위 0.90-1.08 -> 0.93-1.05
    도카이   1.57   1.53        어림으로 적은 대표 구간 16개
    간사이   1.22   1.28        같은 방식 12개
    주고쿠   1.47   1.62        같은 방식 12개

간토에서는 가장 잘 맞는다. 나머지 셋은 대표 구간의 "실제" 값이 어림이라 판단이
어렵다. 도카이·간사이가 느린 까닭은 배차가 아니라 쾌속·신쾌속 속도(간토로 맞춘
84.8km/h)라, 이 데이터로는 안 풀린다.

## 배차·속도·섬 (2026-09-23)

- **배차는 운행 횟수 데이터가 기본.** `data/honsu/unkohonsu2026_kukan.txt` 가 있으면
  쓴다(`NAIVE_HONSU=0` 이나 region.json `"headways": "rule"` 로 끈다). 이 데이터를
  쓰면 `line-headways.json` 은 짝이 없는 구간에만 쓴다.
- **주행 시간을 선로 등급으로.** 7개 권역 완행 100쌍을 조사 에이전트로 모아(평일 낮,
  Yahoo路線情報·ekitan) 한 번에 맞췄다. 간선/지선(usage)은 속도를 못 가르고 전철화가
  갈랐다. 정차 98.7초(도시철도 73.3초), 전철화 79.3km/h, 비전철 60.0, 지하철·모노레일
  87.9, 노면전차 40(법정 최고속도). 권역 하나를 빼고 맞춰 예측해도 쌍마다 ±12%.
  예전 간토식 한 가지 속도는 지방 완행을 17% 빠르게, 쌍마다 ±23% 로 봤다.
  권역별 속도 하나로 맞추면 오히려 ±26% 가 됐다(간선 79km/h 와 로컬선 39km/h 가
  한 권역에 섞인다). `build_track` 이 구간마다 `track-attrs.json` 에 등급을 남긴다.
  간토OSM 은 실제 시각표 대비 1.00 -> 1.04 로 조금 느려졌다.
- **통과 열차는 여전히 느리다(1.05-1.24).** 속도가 아니라 쾌속·특급 계통 데이터가
  없어서다(위키 표를 못 읽는 JR 문서).
- **섬.** 역이 든 땅에서 1km 안의 땅을 "다리로 이어졌다" 고 넣던 것을, 보행망으로
  실제 이어졌는지로 바꿨다. 宮島·似島·答志島·猿島 등 배로만 가는 섬이 빠지고,
  天草·しまなみ海道·とびしま海道 의 섬이 새로 들어왔다.
- **선형.** 土讃線 이 예각 분기역 佃 을 569m 비켜 갔다(다듬기가 깎음). switchbacks.json
  에 적어 막았다.
- **사전.** 磐越東線 いわき, 姫新線 姫路 끝 잇기. 奥羽本線 大館 은 새 항목 "끼울 역" 으로
  넣었다. 小野田線 지선 이름을 "… 本山支線" 으로 바꿔 목록에서 본선 밑으로 접힌다.

## 스위치백·꺾임·경계 (2026-09-23)

- **스위치백 사전이 괄호 붙은 제목에 안 걸렸다.** "富山地方鉄道本線 (電鉄富山=>…)" 처럼
  구간이 붙은 제목은 괄호 앞 이름으로도 찾는다. 실제 스위치백 역을 넣었다: 上市,
  富士山, 姨捨, 二本木, 坪尻·新改, 立野, 大平台. 넣기 전에는 선이 역에서 70-270m 비껴
  갔고, 지금은 0-3m 다.
- **Z 자 꺾임.** 두 구간이 역에서 안 만나면 역 좌표를 거쳐 잇는데, 1.6 배 돌아가는
  것까지 받아 선로에서 20m 비켜 적힌 ODPT 역 좌표로 꺾였다(常磐線 龍ケ崎市). 1.15 배로
  좁히고, 25m 미만 토막 양 끝이 모두 80도 넘게 꺾이는 작은 Z 를 편다. 간토에서 역
  30m 안의 90도 넘는 꺾임이 87곳에서 5곳(대부분 일부러 둔 스위치백)이 됐다.
- **type=railway 관계.** 黒部峡谷鉄道本線 이 route 가 아닌 railway 형식이라 빠졌다.
  추출본 전부에서 이 형식은 이것 하나다.
- **경계 넘는 종점.** 大船渡線 은 열차가 気仙沼(미야기)까지 가는데 북도호쿠 경계에서
  잘렸다. region.json 의 "extra_areas" 에 [경도, 위도, 반경 m] 를 적으면 그 안의 역은
  권역 밖이어도 받는다. 캐시에는 걸러진 뒤의 역만 남아 있어 한 번 다시 훑어야 한다.
- 高山本線 을 富山 까지, 권역 목록은 서버가 지원 역 많은 순으로 보낸다.

---

## 남은 것

### 빌드를 돌려야 반영되는 것

- 日光 역 (정차역 2개 이상인 노선도 빠진 역을 메우게)
- 왕복 중복 마지막 2개 (간사이 大阪環状線, 간토OSM 東急新横浜線)
- 훈련용 모의역 5개, 디즈니 리조트 라인 제외
- 승강장별 좌표 (간토OSM 804개, 간사이 469개 역 줄)
- 銀座線 末広町 이음매
- 구간 선형을 역 쌍으로 찾기 (総武快速線 0개 -> 채워짐)
- 이름 빌려오기 80% 기준

### 아직 손 안 댄 것

- **남의 선로를 타는 문제.** 구간 선형 코버리지가 간토 83% 다. 나머지는
  아무 노선의 선형이나 골라 쓴다. 역 쌍 색인으로 얼마나 오르는지 재야 한다.
- **그 밖에 운영사가 없는 13개.** 이름으로도 못 가린다(東海道本線 이
  JR동일본인지 JR도카이인지). 손으로 적는 표가 필요하다.
- **信越本線 이라는 이름의 관광 트롯코.** 碓氷峠 철도문화마을 것이다.
  역이 とうげのゆ·まるやま·ぶんかむら·横川 넷이다. 진짜 信越本線 도 같은
  권역에 있어 이름으로는 못 버린다. `excluded-lines.json` 에 "역으로
  가리는 것" 을 두고, 적어 둔 역을 모두 들고 있을 때만 버린다.
  `build_rail.py` 는 관계를 읽는 시점에 정차역을 모르므로 `region.py` 가
  올릴 때 본다. 그래서 지도와 디버그 패널에서는 사라지지만 **경로
  그래프에는 남는다.** 완전히 빼려면 build_rail 쪽 구조를 바꿔야 한다.
  진짜 信越本線 쪽도 꼬리에 ぶんかむら 를 하나 물고 있다(1.6 km 라
  reseat_strays 에 안 걸린다).
- **간토OSM 4개 노선의 한국어 이름이 비었다.** 東急新横浜線 (新横浜->日吉),
  西武山口線, 西武鉄道狭山線, 東武小泉線.
- **사이타마 북쪽이 좁다.** 시각표 권역은 宇都宮線 한 번으로 大宮까지 33분인데
  가상 시각표 권역은 京浜東北線 각역정차로 45분이다. 上野東京ライン 같은
  장거리 직통 계통이 `express-wiki.json` 에 없어서 각역정차만 깔린다.
- **폴더명과 GitHub 저장소 이름.** `tokyo-isochrone` -> `rail-isochrone`.
- **커밋.** 이 세션에서 한 번도 안 했다.

### 코드 리뷰가 짚었고 아직 안 고친 것

- `geometry.own_pieces` 등 죽은 코드 130줄.
- `build_walk.py` 의 해안선 way 중복 제거 없음. 추출본 두 개가 겹치는
  권역에서 공유 way 가 두 번 들어간다(간사이 648개, 간토 469개 선분).
- `_operator_rank` 가 노선 id 앞머리로 회사를 보는데 OSM 권역은 그게 거의
  다 `OSM` 이라 판별을 전혀 못 한다.
