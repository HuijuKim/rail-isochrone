# 인수인계

이 문서는 저장소를 처음 보는 사람을 위한 것이다. 코드가 왜 지금 모양인지,
새 나라를 붙이려면 어디를 고쳐야 하는지, 무엇을 확인해야 하는지를 적는다.
2026-09-20 기준이다.

## 무엇을 하는 프로그램인가

출발지와 출발 시각을 주면 n분 안에 전철과 걸어서 닿는 범위를 지도에 그린다.
RAPTOR 로 열차를, 보행망으로 걷기를 계산한다.

## 권역

모든 데이터는 `data/regions/<권역id>/` 안에 갇혀 있다. 권역끼리 공유하는 것은
`data/line-colors.json` 과 `data/excluded-lines.json` 둘뿐이고, 둘 다 노선
이름을 열쇠로 쓰는 추가형이라 새 권역을 만들어도 기존 권역은 다치지 않는다.

지금 있는 권역은 셋이다.

| id | 시각표 | 노선 출처 | 비고 |
|---|---|---|---|
| `kanto` | ODPT | mini-tokyo-3d + OSM | 실제 시각표로 RAPTOR |
| `kanto_osm` | 없음 | OSM | 배차를 추정해 시각표를 지어낸다 |
| `kansai` | 없음 | OSM | 같은 방식 |

권역 설정은 `data/regions/<id>/region.json` 하나다. 이것만 저장소에 올라가고
나머지 산출물은 `.gitignore` 되어 있다.

## 빌드 순서

시각표가 없는 권역(OSM 만으로 만드는 권역)은 이 순서다. `REGION` 환경변수로
권역을 고른다.

```
REGION=<id> python src/build_walk.py      # 보행망·해안선·육지 마스크 (10분 안팎)
REGION=<id> python src/build_admin.py     # 행정경계 -> 권역 경계 (3~7분)
REGION=<id> python src/build_rail.py      # OSM 관계 -> 노선·역 (7~9분)
REGION=<id> python src/build_express.py   # 위키백과에서 통과 계통 정차역
REGION=<id> python src/build_naive.py     # 배차를 추정해 시각표를 짓는다
REGION=<id> python src/build_admin.py     # 역에 현 붙이기 (역 id 가 바뀌었으므로)
REGION=<id> python src/build_track.py     # 선로 그래프로 구간 선형 (2~3분)
python src/build_colors.py                # 노선 색
python src/server.py                      # http://127.0.0.1:5173
```

`build_rail.py` 를 다시 돌려 역 번호가 밀리면 `build_walk.py` 도 다시
돌려야 한다. 보행망이 역 번호로 색인된 도보권을 들고 있어서 어긋나면
서버가 권역을 못 올린다. 다만 역 목록에 걸리는 것은 3단계(역별 도보권)
하나뿐이라 나머지는 아껴 쓸 수 있다.

```
REGION=<id> WALK_REUSE=1 python src/build_walk.py   # 1~2·4 단계를 다시 쓴다
```

같은 추출본일 때만 쓴다. PBF 를 새로 받았으면 그냥 전부 다시 돌린다.

`build_admin.py` 가 두 번 나오는 것은 닭과 달걀 때문이다. `build_rail.py` 는
권역 밖 역을 버리려고 현 경계를 쓰고, `build_admin.py` 는 역에 현을 붙이려고
역 목록을 쓴다. 현 경계 자체는 역과 무관하므로 먼저 한 번 만들어 두면 된다.

시각표가 있는 권역은 `src/fetch_data.py` 와 `src/build_graph.py` 를 쓴다.
`src/build_geometry.py` 는 OSM 권역에서 뽑은 선형을 시각표 권역으로 옮긴다
(`REGION=kanto SOURCE=kanto_osm`).

한 권역을 처음부터 만들면 35~40분쯤 걸린다. 대부분이 `build_rail` 과
`build_admin` 인데 둘 다 2GB 짜리 PBF 를 통째로 훑기 때문이다. 한 번만
훑도록 합치면 10분쯤 줄일 수 있다. 아직 안 했다.

## 새 나라를 붙이려면

`region.json` 을 만들고 OSM 추출본을 `data/osm/` 에 놓는 것으로 시작한다.
그 다음 아래 여섯 파일이 일본어에 매여 있으니 손봐야 한다.

| 파일 | 일본에 매인 것 | 어떻게 바꾸나 |
|---|---|---|
| `build_rail.py` | `KINDS`(特急·快速…), `SHINKANSEN` 애칭, `STRIP` 사업자명, `base_name` | 그 나라 열차 종별과 사업자 표기로 |
| `build_admin.py` | `ADMIN_LEVEL = "4"` (도도부현) | 나라마다 다르다. 한국은 4 가 시·도라 그대로 맞는다 |
| `build_express.py` | ja.wikipedia 의 `駅一覧` 표 | 그 나라 위키의 정차역 표. 없으면 이 단계를 건너뛴다 |
| `build_colors.py` | ja.wikipedia 의 `路線色` | 그 나라 위키의 노선색 항목 |
| `build_naive.py` | `SUBWAY`, `FAST_NAME` 정규식 | 지하철·급행을 가리는 말 |
| `build_track.py` | `norm_line` 의 사업자·종별 떼기 | 노선 이름 정규화 |

OSM 을 직접 읽는 부분(선로 그래프, 해안선, 보행망, 라우팅, 등시선)은 나라와
무관하다. 손댈 필요가 없다.

`region.json` 에 들어가는 것.

```json
{
 "id": "권역 id",
 "center": [경도, 위도],          // 지도 첫 화면
 "zoom": 11,
 "names": {"ja": "", "en": "", "ko": "", "zh-Hans": "", "zh-Hant": ""},
 "model": "naive",                // 시각표가 없으면 naive
 "timetable": null,
 "rail": {"source": "OpenStreetMap", "license": "ODbL 1.0"},
 "osm_files": ["<추출본>.osm.pbf"],   // 권역이 걸치면 여러 개
 "grid": {
  "lon0": 서쪽 끝, "lat0": 남쪽 끝, "lat_ref": 가운데 위도,
  "span_x": 미터, "span_y": 미터,
  "ocean_seeds": [[경도, 위도], ...]   // 바다인 것이 확실한 점들
 },
 "prefectures": ["행정구역 이름", ...],  // OSM admin_level=4 의 name
 "start": {"lon": 0, "lat": 0, "names": {...}}   // 기본 출발역
}
```

`ocean_seeds` 가 중요하다. 해안선으로 육지 마스크를 만들 때 여기서 바다를
번지게 한다. 권역 격자 안에 있으면서 확실히 바다인 점을 골라야 한다. 잘못
잡으면 육지가 100% 나오거나 8% 나온다. `build_walk.py` 가 바다를 못 찾으면
크게 경고한다.

`prefectures` 이름이 OSM 과 하나라도 안 맞으면 `build_admin.py` 가 이유를
밝히고 멈춘다. 조용히 넘어가지 않는다.

## 확인하는 법

```
python -m pytest tests/ -q                      # 단위 + 권역 검사
UPDATE_BASELINE=1 python -m pytest tests/test_regions.py   # 기준선 갱신
```

`tests/test_build_rail.py` 는 데이터 없이 도는 단위 검사다. 전부 실제로
틀렸던 자리를 박아 둔 것이라 지우지 말 것.

`tests/test_regions.py` 는 권역마다 도는 데이터 검사다. 수치는
`tests/baseline.json` 에 적혀 있고 나빠지면 실패한다. 재는 것은 다섯이다.

- 경계선이 닫힌 고리인가
- 그린 선 안이 곧 클릭 가능한 곳인가 (`boundary_disagree`)
- 이웃 역이 20km 넘게 벌어진 노선 수 (`far_neighbour_lines`)
- 노선 하나가 몇 조각으로 끊기는가 (`pieces_per_line`, 1.0 이면 안 끊김)
- 역 수가 줄지 않았는가

2026-09-20 기준선: 조각/노선은 kanto 1.02, kanto_osm 1.02, kansai 1.01.
경계 불일치는 세 권역 모두 0.1% 아래.

## 반드시 지킬 것

**고쳤다고 말하기 전에 서버가 실제로 내보내는 값으로 확인한다.** 코드를
고치고 측정까지 했는데 서버를 안 올려서 화면은 그대로인 일이 하루에 세 번
있었다. Flask 는 `debug=False` 라 코드를 다시 읽지 않는다.

```
python src/server.py > build.log 2>&1        # 로그를 남긴다
curl "http://127.0.0.1:5173/api/railways?region=<id>"
```

**파이프라인을 멈출 때는 자식 프로세스까지 확인한다.** 셸만 죽고 파이썬이
살아남아 같은 파일을 두 번 쓰는 일이 있었다.

```
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Select-Object ProcessId,CreationDate,CommandLine | Format-List
```

**빌드 전에 시뮬레이션한다.** 40분짜리 빌드를 돌리기 전에, 지금 데이터로
그 변경이 무엇을 바꾸는지 세어 본다. "지선으로 승격할 계통이 몇 개인가",
"합치기를 풀면 역이 몇 개 살아나는가" 같은 것은 빌드 없이 셀 수 있다.
그렇게 해서 임계값이 틀린 것을 두 번 잡았다.

## 오늘 정한 상수와 근거

`src/geometry.py`

- `DRAW_GAP_M = 3000` — 이보다 큰 걸음은 선형이 빠진 자리로 보고 끊는다.
  1km 로 두었더니 터널을 잘랐다. OSM 은 긴 터널을 두 점으로 그린다(단나
  터널 7.8km). 세 권역의 호를 재니 0.6~3km 에 324개가 몰려 있고 3km 를
  넘는 것은 3개뿐이라 그 사이에서 갈랐다.
- `LOOP_MIN_M = 3000`, `LOOP_GAP_RATIO = 0.02` — 순환선 판정. 끝 사이
  거리만 절대값으로 보면 1.5km 짜리 곧은 토막이 순환선이 된다. 간토 OSM 판은
  1,570개 중 1,300개가 그랬다.
- `SNAP_TOLERANCE_M = 450` — 역을 선형에 붙이는 한계. 350 일 때 도부
  도조선 쓰키노와가 394m 로 간발의 차로 안 붙어 선이 끊겼다.
- 후보를 고르는 순서: 역에 닿는가 → 성긴가 → 현이 있는가 → 얼마나 돌아가는가
  → **같은 회사인가** → 같은 노선인가 → 앞 구간과 같은 선형인가 → 짧은가.
  "같은 회사" 가 없으면 게이큐 본선이 JR 선로를 따라 그려진다. 게이큐 제
  선형이 도식적이라 역에 안 닿고, 200m 옆 JR 선형 열몇 개가 이긴다.
  "앞 구간과 같은 선형" 을 앞에 두면 한 번 거친 선형을 잡고 안 놓아서
  정밀한 선로 선형이 31% 구간에서만 쓰인다.

`src/region.py`

- `SHAPE_TOLERANCE_M = 3` — 그릴 때 줄이는 정도. 25m 면 야마노테선이
  917점에서 54점으로 줄어 곡선이 직선이 된다.
- `JOIN_TOLERANCE_M = 600` — 구간과 구간 이음매. 실측 최대가 464m 이고
  전부 오사카/기타신치처럼 역 구내다.

`src/build_rail.py`

- `_same_station` 은 이름이 **완전히 같을 때만** 합친다. 글자를 나눠 가지면
  합치던 때 新川崎 와 鹿島田(315m)이 한 점이 되어 신카와사키가 사라지고
  南武線 차례가 꼬였다. 이름이 갈린 환승역은 합치지 않아도 `build_naive` 가
  400m 안이면 환승 간선을 놓아 준다.
- `MIN_BRANCH = 1` — 새 역을 이만큼 가져오는 계통은 통과 계통이 아니라
  지선으로 보고 노선으로 세운다. 2 로 두면 도부 고이즈미선 지선이 안 걸린다.
  竜舞 만 새 역이고 太田 는 이세사키선에 이미 있어서다.
- `SHINKANSEN` 은 애칭을 이름 끝이나 구분 기호 옆에서만 본다. 그냥 찾으면
  ゆりかもめ 가 かもめ 에, ときわ 가 とき 에 걸려 노선째 사라진다.
- `fill_missing` 은 관계에 안 실린 역을 제 선로에 투영해 끼운다. 거르는 것이
  넷이고, 넷 다 실제로 무언가를 망가뜨려서 넣은 것이다.
  1. 후보는 `railway` 가 역인 노드 중 **어느 계통도 정차역으로 안 부른 것**
     뿐이다(`pool`). 이 거름이 없으면 나란히 달리는 남의 노선 역이 온다.
     山陰本線 120m 안에 사가노 관광철도 トロッコ嵯峨 와 京福 撮影所前 이 있다.
  2. 이름이 이미 있는 역과 같으면 넘긴다(浜坂 / 浜坂駅).
  3. `FILL_SAME_M = 200` — 이미 있는 정차역 자리면 넘긴다. 이름 없는
     정차 노드는 이름으로 못 가려서 자리로 가린다. 이것을 빼면 叡山本線 과
     草津線 이 같은 역을 두 번 들고 이웃 간격이 도리어 벌어졌다.
  4. **사슬마다 따로** 세우고, 이미 있는 역 둘 사이에 든 후보만 끼운다.
     사슬을 이어 붙인 거리로 한 줄에 세우면 안 된다. `stitch_all` 이 주는
     사슬은 순서도 방향도 노선을 안 따라서, 제자리를 못 찾은 역이 목록 맨
     앞이나 맨 뒤로 간다. 和歌山線 머리에 薬水, 泉北線 꼬리에 なかもず 가
     그렇게 붙었다.
  `r["kind"]` 가 `None` 이나 `각역정차` 일 때만 부른다. 특급에 부르면
  지나가는 역이 정차역이 된다. 간사이에서 역 이름 39개가 이걸로 살아났고
  (玄武洞·養父·国府·梁瀬·下夜久野·上川口·和知·立木 등), 이웃 간격이 20km
  넘는 계통이 11개에서 9개로, 10km 넘는 계통이 20개에서 15개로 줄었다.

`src/build_admin.py`

- 해안선은 벡터로 자른다. OSM 해안선을 평면에 깔아 면으로 쪼갠 뒤, 어느 면이
  육지인지는 250m 육지 마스크에 묻는다. "해안선은 육지를 왼쪽에 둔다" 는
  OSM 규약으로 가르려 했으나 `coast.npz` 를 만들 때 방향이 뒤집힌 사슬이
  섞여서 못 쓴다. 간사이는 우연히 맞았고 간토는 육지와 바다가 통째로
  뒤바뀌어 역이 2,742개에서 25개로 줄었다.
- `BOUND_TOLERANCE_M = 15` — 판정용 고리와 그리는 선이 같은 값이어야 한다.
  다르면 그린 선 안인데 클릭이 거부되는 띠가 그 차이만큼 생긴다.

## 남은 과제

- **구간별 선형을 저장하지 않는다.** `build_track.py` 가 역과 역 사이를
  선로 그래프에서 찾아 놓고는 한 줄로 이어 붙이며 구간 경계를 버린다. 그래서
  그릴 때 역을 선형 위에 다시 투영해 잘라야 하고, 위의 임계값들이 전부 거기서
  나온다. `(역A, 역B) -> 좌표열` 로 저장하면 그 층이 통째로 없어진다.
  노선 제 선형만 쓰는 방식은 시험해 봤으나 더 나빴다(간토 3곳 → 15곳).
  직통 계통이 남의 선로를 달리기 때문이다. 구간별 저장이 답이다.
- **지선이 한 노선에 Y 자로 들어 있으면 한 줄로 못 그린다.** JR 쓰루미선이
  오기마치·오카와·신시바우라 세 갈래라 2.3km 가 끊긴다. 노선을 갈래로
  나눠야 한다.
- **정차 노드가 없는 역이 아직 남는다.** `fill_missing` 이 대부분을 주웠지만,
  제 사슬에 이미 있는 역이 둘이 안 되면 방향을 못 정해 건너뛴다. 山陰本線 의
  佐津·柴山·石原·餘部 가 그렇게 남았다. 사슬끼리 끝점을 맞춰 노선 순서대로
  이으면 풀린다.
- **阪神電気鉄道本線 은 尼崎センタープール前 이 맨 앞에 온다.** 이웃 간격이
  9.5km 로 잡힌다. `fill_missing` 을 꺼도 그대로라 원인은 다른 데 있다.
  `MIN_BRANCH` 와 `OUTLIER_MULT` 를 바꾼 뒤에 생겼으니 거기부터 본다.
  京阪本線 도 같은 모양이고, 대신 阪急宝塚本線 과 草津線 은 이번에 풀렸다.
- **노선 id 슬러그가 불안정하다.** 사업자 이름에서 ASCII 만 남겨서 간사이
  143개 중 141개가 `OSM` 으로 뭉치고 밑줄 개수로 구별된다. 그 순서가 정렬에
  의존해서 추출본이 갱신되면 id 가 조용히 옮겨 붙는다.
- **화면의 출처 표시가 고정되어 있다.** 시각표가 없는 권역을 골라도
  "시각표 출처 mini-tokyo-3d" 가 뜨고, 위키백과 CC BY-SA 표기가 안 나온다.
