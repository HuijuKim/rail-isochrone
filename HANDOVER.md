# 인수인계

이 문서는 저장소를 처음 보는 사람을 위한 것이다. 코드가 왜 지금 모양인지,
새 나라를 붙이려면 어디를 고쳐야 하는지, 무엇을 확인해야 하는지를 적는다.
무엇을 언제 왜 고쳤는지의 긴 기록은 `WORKLOG.md` 에 있다.
2026-09-21 기준이다.

## 무엇을 하는 프로그램인가

철도 등시선(Rail Isochrone). 출발지와 출발 시각을 주면 n분 안에 전철과
걸어서 닿는 범위를 지도에 그린다. RAPTOR 로 열차를, 보행망으로 걷기를
계산한다. 화면은 5개 언어(ko·ja·en·zh-Hans·zh-Hant)다.

저장소는 `github.com/HuijuKim/rail-isochrone` 이다. 로컬 폴더는 아직
`tokyo-isochrone` 인데, VS Code 가 작업 공간으로 열고 있으면 잠겨서 이름을
못 바꾼다. 창을 닫고 바꾸면 된다. git 설정은 폴더 안에 있어 따라간다.

## 권역

권역마다 데이터는 `data/regions/<권역id>/` 안에 갇혀 있다. 권역끼리 공유하는
것은 `data/` 바로 아래의 사전들이다. 전부 일본어 이름을 열쇠로 쓰는 추가형이라
새 권역을 만들어도 기존 권역은 다치지 않는다. 손으로 고치면 서버만 다시
올리면 된다.

| 파일 | 담는 것 |
|---|---|
| `line-names.json` | 노선 이름 5개 언어. **이 파일이 원본 데이터를 이긴다** |
| `station-names.json` | 역 이름 5개 언어. 비어 있는 언어만 채운다 |
| `line-operators.json` | 태그도 이름도 회사를 알려 주지 않는 노선의 회사. 권역별 |
| `line-colors.json` | 노선 색. `build_colors.py` 가 만들고 손으로 고친다 |
| `excluded-lines.json` | 타고 다닐 수 없는 노선(화물·폐선·관광 삭도·유원지) |
| `line-extensions.json` | OSM 이 시설 기준으로만 적어 실제 운행과 끊긴 노선 끝, 두 갈래 선로 중 뺄 쪽 역("빼는 역"). 권역별 |
| `switchbacks.json` | 열차가 방향을 바꾸는 역. 지도 선이 거기서 깎이지 않게 |
| `line-headways.json` | 규칙으로 매긴 배차를 실제에 맞춰 누르는 노선별 하한·상한. 권역별, 출처를 함께 적는다 |

지금 있는 권역은 다섯이다.

| id | 시각표 | 노선 출처 | 비고 |
|---|---|---|---|
| `kanto` | ODPT | mini-tokyo-3d + OSM | 실제 시각표로 RAPTOR |
| `kanto_osm` | 없음 | OSM | 배차를 추정해 시각표를 지어낸다 |
| `kansai` | 없음 | OSM | 같은 방식 |
| `tokai` | 없음 | OSM | 같은 방식. 아이치·기후·시즈오카·미에. 추출본 셋(주부·간사이·간토)을 읽는다 |
| `kyushu` | 없음 | OSM | 같은 방식. 규슈 7현과 야마구치. 추출본 둘(규슈·주고쿠)을 읽는다 |

앞으로 붙일 권역은 이렇게 나누기로 했다(2026-09-21). 현을 통째로 묶고,
격자는 300km 사방 안쪽으로 둔다. 홋카이도와 오키나와는 철도망이 성겨 뺀다.

    남도호쿠 미야기·야마가타·후쿠시마      북도호쿠 아오모리·이와테·아키타
    고신에쓰 야마나시·나가노·니가타        호쿠리쿠 도야마·이시카와·후쿠이
    주고쿠 5현(야마구치 제외)   시코쿠 4현   규슈 7현 + 야마구치(만들었다)

권역 설정은 `data/regions/<id>/region.json` 하나다. 이것만 저장소에 올라가고
나머지 산출물은 `.gitignore` 되어 있다.

## 빌드 순서

시각표가 없는 권역(OSM 만으로 만드는 권역)은 이 순서다. `REGION` 환경변수로
권역을 고른다. **서버를 멈춘 뒤에 돌린다.** 서버가 보행망 파일을 mmap 으로
물고 있어 윈도우가 덮어쓰기를 막는다.

```
REGION=<id> python src/build_walk.py      # 처음 한 번. 보행망·해안선·육지 마스크
REGION=<id> python src/build_admin.py     # 처음 한 번. 행정경계 -> 권역 경계

REGION=<id> python src/build_rail.py      # OSM 관계 -> 노선·역
REGION=<id> python src/build_express.py   # 위키백과에서 통과 계통 정차역
REGION=<id> python src/build_track.py     # 선로 그래프로 역-역 구간 선형
REGION=<id> python src/build_naive.py     # 배차를 추정해 시각표를 짓는다
REGION=<id> python src/build_admin.py     # 역에 현 붙이기
REGION=<id> python src/build_walk.py      # 역별 도보권 (역 번호가 바뀌었으므로)
python src/build_colors.py                # 노선 색
python src/server.py                      # http://127.0.0.1:5173
```

순서에 이유가 있다.

- **`build_rail` 뒤에는 반드시 `build_track`.** OSM 은 다시 훑을 때마다 역
  id 를 새로 매긴다. `build_track` 을 안 돌리면 구간 선형이 옛 id 를 들고
  남아, 오류 하나 없이 선이 사라진다(芝山鉄道線 이 그랬다).
- **`build_track` 이 `build_naive` 앞.** `build_naive` 가 구간 선형으로 역간
  거리를 재서 소요 시간을 짓는다.
- **`build_admin` 은 `build_naive` 뒤.** 현을 붙이는 대상은 `build_naive` 가
  쓰는 `stops.json` 이다. 앞에서 돌리면 옛 역 목록에 붙는다. 같은 추출본이면
  역 번호가 같아 티가 안 나지만, 처음 만드는 권역은 현이 전부 비었다.
- **`build_walk` 는 맨 뒤.** 보행망이 역 번호로 색인된 도보권을 들고 있어서,
  역 목록이 바뀌면 어긋나 서버가 권역을 못 올린다.
- **`build_express` 는 처음에 위키 문서 이름을 인자로 준다.** 받은 표는
  `raw/wiki-stops.json` 에 남아, 다음부터는 인자 없이 돌리면 그것을 다시 붙인다.
  도카이에서 준 목록은 그 파일의 열쇠에 있다. 종별이 하나뿐인 표(지선 대부분)는
  못 읽는다고 나오는데, 모든 열차가 모든 역에 서는 것과 같아 괜찮다.

**처음 만드는 권역.** `build_walk` 와 `build_admin` 이 둘 다 `stops.json` 을
요구하는데 그 파일은 `build_naive` 가 만든다. 역 하나짜리 임시 목록을 두고
보행망·현 경계를 먼저 뽑는다. 임시 목록은 `build_naive` 가 덮는다.

```
echo '{"ids":["STUB"],"coords":[[경도,위도]]}' > data/regions/<id>/stops.json
REGION=<id> python src/build_walk.py
REGION=<id> python src/build_admin.py
# 그 다음 위의 build_rail 부터. build_walk 는 WALK_REUSE=1 로
```

시각표가 있는 권역(`kanto`)은 이렇다.

```
REGION=kanto python src/build_graph.py                        # 시각표 -> 그래프
REGION=kanto TRACK_ONLY=1 python src/build_track.py            # ODPT 역을 OSM 선로에 직접 붙인다
REGION=kanto SOURCE=kanto_osm python src/build_geometry.py     # OSM 권역에서 선형·이름 빌려오기
```

`TRACK_ONLY=1` 은 필수다. 없으면 ODPT 원본인 `coordinates.json` 을 덮는다.

### 다시 쓰는 캐시

같은 PBF 로 로직만 고쳐 다시 돌릴 때를 위한 것이다. 셋 다 켤 때만 쓴다.
**추출본을 새로 받았으면 그냥 전부 다시 돌린다.**

```
RAIL_REUSE=1    build_rail   PBF 세 번 훑은 결과(raw/osm-extract.*). 8~10분 -> 수십 초
ADMIN_REUSE=1   build_admin  행정경계와 권역 경계(raw/admin-polygons.npz). 3분 -> 2초
WALK_REUSE=1    build_walk   보행망과 육지 마스크. 역별 도보권만 다시. 5분 반 -> 1분
```

`RAIL_REUSE` 는 PBF·권역 경계의 크기와 수정 시각을 함께 적어 두고,
하나라도 바뀌면 저장해 둔 것을 버리고 다시 훑는다. 제외 목록(`excluded-lines.json`)은
캐시를 읽을 때 걸러서, 고쳐도 다시 훑지 않는다. 캐시 파일은 `build_rail` 을
캐시 없이 한 번 돌려야 생긴다. 종별은 캐시를 읽을 때 이름에서 다시 매기므로,
`KINDS` 나 특급 애칭 목록만 고쳤으면 다시 훑지 않아도 된다. 역 이름 다듬기
(`clean_station_name`)도 읽을 때 다시 거친다. 2026-09-21 에
처음 돌려 보고 캐시에서 읽은 선로 좌표(numpy 배열)를 참거짓으로 따지던
자리를 고쳤다. 지금은 OSM 권역 넷 모두 캐시가 있다.

두 권역을 **나란히** 돌리면 벽시계 시간이 절반이다. 읽는 PBF 와 쓰는 폴더가
다르다. 메모리는 `build_rail` 한 개가 1GB 남짓 쓴다. 여유가 4GB 아래면
`build_walk` 만 순서대로 돌린다.

    처음 (순서대로, 캐시 없음)           38분
    나란히 + 보행망·행정경계 캐시        13분

`build_admin.py` 가 두 번 나오는 것은 닭과 달걀 때문이다. `build_rail.py` 는
권역 밖 역을 버리려고 현 경계를 쓰고, `build_admin.py` 는 역에 현을 붙이려고
역 목록을 쓴다.

## 새 나라를 붙이려면

`region.json` 을 만들고 OSM 추출본을 `data/osm/` 에 놓는 것으로 시작한다.
그 다음 아래 파일들이 일본어에 매여 있으니 손봐야 한다.

| 파일 | 일본에 매인 것 | 어떻게 바꾸나 |
|---|---|---|
| `build_rail.py` | `KINDS`(特急·快速…), `SHINKANSEN`·`LTD_EXPRESS` 애칭, `STRIP` 사업자명, `base_name` | 그 나라 열차 종별과 사업자 표기로 |
| `build_admin.py` | `ADMIN_LEVEL = "4"` (도도부현) | 나라마다 다르다. 한국은 4 가 시·도라 그대로 맞는다 |
| `build_express.py` | ja.wikipedia 의 `駅一覧` 표 | 그 나라 위키의 정차역 표. 없으면 이 단계를 건너뛴다 |
| `build_colors.py` | ja.wikipedia 의 `路線色` | 그 나라 위키의 노선색 항목 |
| `build_naive.py` | `SUBWAY`, `FAST_NAME` 정규식 | 지하철·급행을 가리는 말 |
| `build_track.py` | `norm_line` 의 사업자·종별 떼기 | 노선 이름 정규화 |
| `operators.py` | 회사 표(`RAIL_OPERATORS`)와 별칭 | 그 나라 회사를 5개 언어로 |
| `region.py` | `_NAMED_TRAIN`, `_KIND_ONLY`, 회사 떼기 목록 | 열차 애칭과 종별 |

OSM 을 직접 읽는 부분(선로 그래프, 해안선, 보행망, 라우팅, 등시선)은 나라와
무관하다. 손댈 필요가 없다.

`region.json` 에 들어가는 것.

```json
{
 "id": "권역 id",
 "center": [경도, 위도],          // 지도 첫 화면
 "zoom": 11,
 "names": {"ja": "", "en": "", "ko": "", "zh-Hans": "", "zh-Hant": ""},
 "model": "naive",                // 시각표가 없으면 naive
 "timetable": null,
 "rail": {"source": "OpenStreetMap", "license": "ODbL 1.0"},
 "osm_files": ["<추출본>.osm.pbf"],   // 권역이 걸치면 여러 개
 "grid": {
  "lon0": 서쪽 끝, "lat0": 남쪽 끝, "lat_ref": 가운데 위도,
  "span_x": 미터, "span_y": 미터,
  "ocean_seeds": [[경도, 위도], ...]   // 바다인 것이 확실한 점들
 },
 "prefectures": ["행정구역 이름", ...],  // OSM admin_level=4 의 name
 "start": {"lon": 0, "lat": 0, "names": {...}}   // 기본 출발역
}
```

`ocean_seeds` 가 중요하다. 해안선으로 육지 마스크를 만들 때 여기서 바다를
번지게 한다. 잘못 잡으면 육지가 100% 나오거나 8% 나온다. `build_walk.py` 가
바다를 못 찾으면 크게 경고한다.

`prefectures` 이름이 OSM 과 하나라도 안 맞으면 `build_admin.py` 가 이유를
밝히고 멈춘다. 조용히 넘어가지 않는다.

## 확인하는 법

```
python -m pytest tests/ -q                      # 단위 + 권역 검사 (80개)
UPDATE_BASELINE=1 python -m pytest tests/test_regions.py   # 기준선 갱신
```

`tests/test_build_rail.py` 는 데이터 없이 도는 단위 검사다. 전부 실제로
틀렸던 자리를 박아 둔 것이라 지우지 말 것.

`tests/test_regions.py` 는 권역마다 도는 데이터 검사다. 수치는
`tests/baseline.json` 에 적혀 있고 나빠지면 실패한다. 경계가 닫혔는가, 그린
선 안이 곧 클릭 가능한 곳인가, 이웃 역이 20km 넘게 벌어진 노선 수, 노선이
몇 조각으로 끊기는가, 역 수가 줄지 않았는가를 잰다.

2026-09-21 현재 수치.

    권역      구간 선형   되꺾임   역     운영사 없음  노선·역 이름 한국어
    kanto      98.8%      6     2,583      0          100%
    kanto_osm  99.5%      2     2,764      0          100%
    kansai     99.8%      2     2,158      0          100%
    tokai      99.8%      -     1,220      0          100%     (되꺾임은 안 쟀다)

## 반드시 지킬 것

**고쳤다고 말하기 전에 서버가 실제로 내보내는 값으로 확인한다.** Flask 는
`debug=False` 라 코드를 다시 읽지 않는다. 데이터 사전(`data/*.json`)을 고쳐도
서버를 다시 올려야 한다.

```
curl "http://127.0.0.1:5173/api/railways?region=<id>"
```

**재는 자부터 의심한다.** 이 저장소에서 가장 비싸게 배운 것이다. "역에서
선이 Z 자로 꺾인다" 를 며칠 쫓았는데, 마지막에 허용오차를 0 으로 두고 재 보니
선로에 꺾인 자리는 세 권역 통틀어 4곳뿐이었다. 나머지는 그리기 전 단순화가
만든 각이었다. "선이 역에서 837m 떨어졌다" 도 선이 아니라 가장 가까운
꼭짓점까지를 잰 잘못이었다. 수치가 이상하면 원인을 파기 전에 잣대를 한 번
다른 방식으로 재 본다.

**빌드 전에 시뮬레이션한다.** 빌드를 돌리기 전에, 지금 데이터로 그 변경이
무엇을 바꾸는지 세어 본다. 정차 순서 고치기는 빌드 없이 41개 노선이 바뀌고
길어지는 것은 0개인 것을 먼저 확인했다.

**파이프라인을 멈출 때는 자식 프로세스까지 확인한다.** 셸만 죽고 파이썬이
살아남아 같은 파일을 두 번 쓰는 일이 있었다.

```
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Select-Object ProcessId,CreationDate,CommandLine | Format-List
```

**`config.json` 은 절대 올리지 않는다.** 구글 지도 API 키가 들어 있다.
`.gitignore` 되어 있지만 커밋 전에 `git diff --cached | grep AIza` 로 확인한다.

## 코드가 왜 이 모양인가

### 선형 (지도에 그리는 선)

`build_track.py` 가 역에서 역까지 OSM 선로 그래프로 길을 찾아
`raw/track-segments.json` 에 `(노선, 역, 역) -> 좌표열` 로 저장한다. 그리는
쪽(`region.railway_shapes`)은 그걸 이어 붙이기만 한다.

- **구간을 따로 저장하는 이유.** 한 줄로 이어 붙인 뒤 역을 다시 투영해 자르면
  역 근처에서 선이 모서리를 질러간다. 별도 파일인 것은 `coordinates.json` 에
  두면 `build_rail` 이 덮어써서 오류 없이 사라지기 때문이다.
- **열쇠에 노선을 넣는 이유.** 역 쌍만으로 묶으면 같은 역 쌍을 지나는 다른
  노선끼리 덮어쓴다(日暮里-西日暮里 를 셋이 쓴다).
- **제 선로를 먼저 찾는다.** 노선 이름이 붙은 웨이만 모은 그래프에서 먼저
  찾고, 못 찾으면 남의 선로에 4배 벌점(`OTHER_TRACK_COST`)을 준 그래프에서
  찾는다. 막지는 않는다. 직통운전은 실제로 남의 선로를 달린다.
- **괄호 안 이름도 본다(`line_keys`).** OSM 은 사이쿄선 선로를
  `東北本線（埼京線）` 이라 적는다. 괄호를 버리면 사이쿄선이 제 선로를
  한 구간도 못 찾는다.
- **역을 역 줄마다 따로 선로에 붙인다(`snapped_sid`).** 묶음 하나로 뭉개면
  다층 역에서 남의 노선 선로에 붙는다. 武蔵浦和 는 무사시노선이 지상,
  사이쿄선이 고가다.
- **구간끼리 같은 노드를 공유한다.** 앞 구간이 끝난 노드에서 다음 구간을
  시작한다. 따로 고르면 한 역에서 중앙 166m 어긋나 삼각형이 그려진다.

그린 뒤에 세 가지를 더 한다. 전부 `geometry.py` 다.

- `_trim_overshoot` — 구간을 제 두 역 사이로 자른다. 역을 지나쳐 나갔다
  돌아오는 꼬리를 없앤다.
- `unwind_retrace` — 이음매에서 앞 구간 끝과 뒤 구간 시작이 같은 길을
  되짚으면 걷어낸다.
- `smooth_spikes` — 갔다가 되돌아왔다 다시 가는 점을 뺀다. 다만 점이 역에
  30m 안으로 붙어 있고 빼면 선이 그 역에서 200m 넘게 떨어지면 둔다. 하코네
  등산철도 大平台 같은 진짜 스위치백을 지키는 조건이다. 넓게 잡으면 되꺾임이
  대개 역 근처라 정상 제거까지 막혀 더 나빠진다(재 보고 정한 값이다).

이음매 되짚기 걷어내기(`unwind_retrace`)는 스위치백도 걷어낸다. 養老線 은
大垣 에서 방향을 바꿔 같은 선로를 900m 오가는데, 그걸 걷어내 선이 大垣 에 닿지
않았다. 규칙(걷어낸 뒤 끝이 역에서 멀어지면 취소)으로 가르려 하니 역을 지나쳤다
돌아오는 인공 꼬리까지 되살아나 42~95개 노선이 바뀌어, `data/switchbacks.json` 에
노선과 역을 적어 그 이음매만 건너뛴다. `smooth_spikes` 도 같은 일을 할 수 있어,
정리 전에 역 30m 안을 지나던 선이 정리 뒤 200m 넘게 멀어지면 그 역에 가장 가까운
원래 점을 고정하고 다시 정리한다(단독으로는 네 권역 선형을 하나도 안 바꿨다).

마지막으로 `region._simplify` 가 점을 줄인다(`SHAPE_TOLERANCE_M = 3`). 줄인
뒤 100도 넘게 꺾여 보이는 자리는 원본을 8분의 1 허용오차로 되살린다
(`SIMPLIFY_TURN_DEG`). 반경 작은 곡선을 3m 로 줄이면 부드러운 곡선이
날카로운 각이 되기 때문이다. 허용오차를 통째로 낮추면 점이 76% 는다.

### 정차 순서

OSM 관계의 정차 순서는 자주 틀린다. 두 단계로 고친다(`build_rail.py`).

- `repair_order` — 상행·하행이 한 줄에 섞인 왕복 중복을 한 방향으로 편다.
  순환선은 첫 역과 끝 역이 같으면 손대지 않는다. 펴는 방법을 셋 만들어
  **순서대로 이은 길이가 가장 짧은 것**을 쓴다. 처음 나온 순서로 지우기(예전
  방식), 가는 반을 뼈대로 돌아오는 반에만 있는 역 끼우기(近鉄名古屋線 은
  하행에 白子 남쪽이 빠져 있어 예전 방식이 그 역들을 伊勢中川 뒤로 거꾸로
  붙였다), 같은 역은 이웃과 가장 잘 맞는 자리만 남기기(`_drop_strays`,
  紀勢本線 앞머리의 떠돌이 下里·那智).
- `reseat_strays` — **노선은 길이라, 역을 순서대로 이은 길이가 짧을수록
  옳은 순서다.** 한 역을 빼서 가장 덜 돌아가는 자리에 다시 넣기를 전체 길이가
  1% 넘게 줄 때만 되풀이한다. 버리지는 않는다. 자리만 틀렸을 뿐 진짜 역이다.
  成田線 52.5km -> 33.9km, 常磐線 최대 이웃 간격 60.1km -> 7.2km.
  다만 **역들이 제 선로 사슬 위에 한 방향 차례로 놓여 있거나(`_follows_track`),
  출발역으로 돌아오는 닫힌 관계면 옮기지 않는다.** 선로가 고리처럼 도는 노선은
  틀린 자리가 몇 % 짧게 나와 옳은 순서를 망가뜨렸다(ゆりかもめ 의 お台場海浜公園
  가 青海 옆으로, ユーカリが丘線 의 井野 가 公園 옆으로). 튀는 간격으로 가르려 하니
  成田線·相模線 같은 진짜 오류까지 못 고쳐 되돌렸다.
  라켓 모양(줄기 → 고리 → 같은 줄기로 되짚기)의 닫힌 관계는 고리를 처음 닫는
  곳까지 쓴다(ユーカリが丘線 … 井野 公園, ポートライナー … 北埠頭 中公園). 중복을
  다 지우면 고리의 마지막 구간이 빠진다. 고리 뒤가 줄기를 정확히 거꾸로 되짚을
  때만이다. 간사이의 近鉄名古屋線 은 미에 쪽만 잘린 목록이 우연히 닫혀 있다.

`fill_missing` 은 관계에 안 실린 역을 **노선 길이가 가장 적게 늘어나는
자리에** 끼운다(`_insert_cheapest`). 예전에는 선로를 끝점끼리 이은 사슬 위에서
아는 역 사이에 끼웠는데, 역 구내 측선 때문에 사슬이 잘게 쪼개지면(関西本線
191개) 아는 역이 없는 사슬의 역은 버려졌다(新堂·佐那具·伊賀上野·島ヶ原).

후보는 이렇게 고른다.

- **어느 계통도 정차역으로 안 부른 역**만. 이 거름이 없으면 나란히 달리는
  남의 노선 역이 딸려 온다(山陰本線 옆 사가노 관광철도 トロッコ嵯峨).
  `usage` 가 tourism·training·military 등이면 뺀다. 도쿄에 JR동일본 승무원
  훈련용 모의역(志茂田·大田)이 `railway=station` 으로 있다. 이름 없는 노드도 뺀다.
- 다만 **특급만 부른 역은 후보에 둔다.** 특급 정차역은 그 선로의 완행역이기도
  해서, 거르면 高山本線 이 ひだ 가 서는 下呂·高山 등을 못 줍는다. 쾌속·급행이
  부른 역은 그대로 거른다. 総武快速線 처럼 제 선로를 가진 쾌속이 있어, 풀면
  新日本橋 가 옆의 銀座線 에 붙는다(시뮬레이션으로 잡았다).
- 제 선로 200m 안(`FILL_NEAR_M`)이어도 **가장 가까운 선로가 남의 것이면 한가운데로는
  안 받는다**(`_nearest_track`). 어느 계통도 안 부른 남의 역이 제 선로 옆에 있을 때
  거른다. 近鉄名古屋線 에 JR 長島, 東海道本線 에 樽見鉄道 東大垣, 紀勢本線 에
  近鉄 東松阪, JR和歌山線 에 近鉄 薬水 가 딸려 와 있었다. 제 선로 60m 안이면 노선
  끝에는 붙인다(分岐駅 종점. 参宮線 의 多気 는 가장 가까운 선로가 紀勢本線 이다).

같은 노선으로 묶인 계통(90% 넘게 겹침)은 가장 긴 것 하나가 뼈대가 된다. 다른
계통에만 있는 역은 **뼈대의 끝에만** 붙인다(近鉄山田線 의 종점 宇治山田). 한가운데까지
받으면 묶인 계통의 오류가 딸려 온다(きのさき 가 품은 西舞鶴 가 山陰本線 에, 空港線
계통의 関西空港 이 南海本線 에). 끝에도 못 붙는 역이 그 계통의 한쪽 끝에 이어져
있고 역 사이가 5km 안쪽이면 지선으로 세운다(豊橋鉄道 의 運動公園前).

**특급 애칭은 특급으로 본다(`LTD_EXPRESS`).** 이름에 特急 이 없는 踊り子·
ひだ·しなの·はまかぜ 같은 계통이 종별 없는 노선으로 읽히면, 정차역이 많아
뼈대로 먼저 뽑히고 진짜 노선을 부분 계통으로 밀어낸다. 伊東線 이 踊り子 밑으로
들어가 熱海·宇佐美·伊東 를 잃었다. 여러 노선에 걸쳐 달려 어느 노선과도 70%
넘게 겹치지 않는 특급(はまかぜ·こうのとり)은 여전히 노선으로 서고, 지도에서는
`_NAMED_TRAIN` 이 가린다.

**선로 관계만 있는 노선은 선로 옆 역을 줍는다.** 樽見鉄道 는 계통 관계 없이
`route=railway` 만 있고 역이 전부 선로 옆 `railway=station` 이라, 선로 위
노드로만 찾으면 大垣 하나만 잡혀 노선째 빠졌다. 간선의 선로 관계가 남의 역을
줍지 않도록 위와 같은 후보(어느 계통도 안 부른 역)만 본다.

**끊긴 노선 끝은 사전으로 잇는다(`line-extensions.json`).** OSM 은 노선을
시설 기준으로 적는다. 名鉄犬山線 은 新鵜沼-下小田井 인데 열차는 역이 아닌
枇杷島分岐点 에서 본선으로 들어가 名鉄名古屋 까지 간다. 두 노선이 어느 역에서도
만나지 않아 나고야에서 이누야마까지 기후를 돌아 80분이 나왔다. 長良川鉄道 는
관계가 前平公園 에서 끝나 分岐駅 美濃太田 가 빠졌고, 名鉄小牧線 은 上飯田 에서
平安通 까지 직통한다. 다른 계통이 부르는 역까지 노선 끝 후보로 넓혀 봤더니
버스 정류장 노드(京成バスセンター 등)가 줄줄이 붙고 常磐線 뼈대가 흔들려 되돌렸다.
그래서 손으로 적는다.

**선로 관계가 뼈대인 노선은 세 가지를 더 본다(규슈에서 나왔다).**
- 완행 계통이 뼈대의 이웃한 두 역 사이에 역을 더 적어 두었으면 끼운다
  (`_bridge`). 계통이 OSM 에 직접 적은 정차역만 옮긴다. 日豊本線 의 都城 등 6역.
- 편도 계통 끝에 출발역이 또 적혀 있고 그 닫는 간격만 튀면 순환선이 아니다
  (`_false_close`). 西鉄天神大牟田線 이 62km 를 되돌아갔다. 이건 계통 관계에도 쓴다.
- 이웃 역이 40km 넘게 떨어지면 두 노선으로 가른다(`split_far`). 鹿児島本線 이
  肥薩おれんじ鉄道 구간 82km 를 한 번에 건넜다. 계통 관계는 애칭 목록에 없는
  특급이 걸려서 뺐다.

### 이름

노선 이름은 `line-names.json` 이 이긴다. 그 다음 ODPT 가 가진 5개 언어를
역 목록이 80% 넘게 겹치는 OSM 노선에 빌려주고(`build_geometry`), 그래도
비면 OSM 태그를 쓴다. 빌려주기 기준을 느슨하게 하면 東武日光線 이 JR日光線
의 "닛코선" 을 받아 간다.

올릴 때(`region.load`) 이름을 이렇게 다듬는다.

- 끝의 경로 설명 `(A → B)`, `: A→B`, `(A-B)` 를 뗀다. 구간은 디버그 패널이
  따로 적는다. `(아라카와선)` 같은 딴이름은 남긴다.
- 일본어 이름에 종별이 **띄어** 적혀 있을 때만 끝의 `보통`·`쾌속` 을 뗀다.
  ODPT 의 `常磐線快速`·`常磐線各駅停車` 는 붙여 적고 그 자체가 노선 이름이라,
  떼면 둘 다 "조반선" 이 된다.
- 앞의 회사 이름을 뗀다. 긴 이름부터 본다("한큐" 를 먼저 떼면 "한큐전철
  교토선" 이 "전철 교토선" 이 된다). 공백은 양쪽 다 지우고 견준다("욧카이치
  아스나로철도" 처럼 띄어 적힌 회사). 뗀 뒤 "본선"·"선"·"1호선" 만 남으면
  되돌린다(山陽本線, 芝山鉄道線, 千葉モノレール1号線). 회사 이름이 노선
  이름의 일부인 것은 사전에 `"keep": true` 를 단다(京王線 게이오선, 西武園線 세이부엔선).
- 이름이 겹치면 회사를 다시 붙이되 **JR 에는 안 붙인다.** JR 노선이 너무
  많아 다 붙이면 목록이 회사 이름으로 도배된다. JR 회사끼리 겹칠 때만 붙인다.

역 이름 한국어는 외래어 표기법을 따른다(千葉 지바, 中央 주오). 철도 동호인
관례(치바, 츄오)와 다르지만, ODPT 가 주는 공식 한국어가 이 표기라 역 2,610개와
맞추려면 이쪽이어야 한다. 간사이 역 이름은 헵번 로마자를 외래어 표기법으로
옮기는 변환기로 채웠다. 이미 한국어가 있는 1,241쌍에 견줘 92.9% 가 맞았다.
도카이 134개도 같은 방식으로 채웠다(변환기를 다시 짜서 사전의 1,944쌍에 94.1%).
규슈 194개도 같다(영어 이름이 없는 역·영어 단어로 적힌 역 50여 개는 손으로).
OSM `name:ko` 에는 "하카타 역", "도스 시" 같은 꼬리가 붙어 있어 `clean_station_name`
이 뗀다. 筑紫 가 "사쿠라다이", 江北 가 옛 이름 "히젠야마구치" 처럼 아예 틀린 것도
있지만 사전은 비어 있는 언어만 채우므로 못 고친다. 같은 일본어 이름의 다른 역
(三郷, 神戸)이 있어 사전이 태그를 이기게 하면 멀쩡한 역이 틀어진다.
변환기는 저장소에 없다. 일본어로 한 단어면 영어의 띄어쓰기를 무시하고 한
단어로 옮긴다(名城公園 메이조코엔, "메이조 고엔" 이 아니다).

### 회사

회사 이름은 `operators.RAIL_OPERATORS` 한 표에서만 가져온다. 시각표 권역은
노선 id 앞머리를 `PREFIX_COMPANY` 로 이 표에 잇는다. 둘이 따로 놀던 때는
같은 회사가 권역마다 "도에이" / "도영지하철" 로 갈렸다.

태그가 없는 노선은 이름 안의 회사(`operator_in_name`) → `line-operators.json`
→ JR 로 시작하면 그 권역에서 흔한 JR → 역을 절반 넘게 공유하는 노선의
회사 순으로 메운다. 손으로 적은 것이 짐작보다 먼저다. 도카이의 JR伊東線 은
흔한 JR(JR도카이)로 짐작되지만 JR동일본이다.

### 걸어서 갈아타기

`transfers.py` 를 `build_naive` 와 `build_graph` 가 같이 쓴다. 1km 까지 잇되
비용을 거리에서 매긴다: `max(300초, 90초 + 거리 × 1.25 / 도보속도)`.
高槻市-高槻 520m 가 9.6분이다. 범위만 넓히고 비용을 고정으로 두면 1km 를
5분에 걷는 셈이 되어 도달권역이 거짓으로 부푼다.

### 지도에 안 그리는 것

`region._is_named_train` 에 걸리는 계통은 제 역이 다른 노선에 95% 넘게 들어
있을 때 지도에서 뺀다. 열차 애칭(えのしま·ひたち·特急はるか)과 종별만 적힌
것(小田急通勤急行, 相鉄直通線, …直通運転)이다. 시각표에는 그대로 남아 경로
계산은 바뀌지 않는다.

`excluded-lines.json` 의 "역으로 가리는 것" 은 이름이 같은 진짜 노선이 있어
이름으로는 못 버리는 것이다(碓氷峠 관광 트롯코가 "信越本線" 이다). 적어 둔
역을 모두 들고 있을 때만 버린다. `build_rail` 은 관계를 읽을 때 정차역을
몰라 여기서 못 걸러서, 지도에서는 빠지지만 경로 그래프에는 남는다.

### 노선 디버그 패널

지도 오른쪽 위. 회사로 묶고, 이름이 같은 노선은 한 줄로 묶어 구간으로 편다.
지선(` 지선` 으로 끝나는 것)은 본선 밑으로 접는다. 띄어쓰기를 요구하는 것은
御堂筋線(미도스지선)·宇治線(우지선) 처럼 이름 끝이 우연히 "지선" 인 것을
걸러내기 위해서다. 검색은 한글 자모로도 된다(`ㅇ`, `얌` 으로 야마노테선).

### 배차

`build_naive` 는 구간마다 주변 역 밀도(d5, 가까운 역 5개까지의 거리)로 등급을
매겨 배차를 정한다. 예전에는 "역 간격 4km 이상이면 한 등급 낮춘다" 규칙이
있었는데, 도카이의 東海道本線 시즈오카 구간(실제 시간당 3~4대)이 한 시간에 한 대로
떨어져 69분 걷는 길을 먼저 권했다. 규칙을 빼고 남는 어긋남은
`line-headways.json` 으로 누른다. 노선마다 시간당 최소·최대 대수를 적고,
값마다 출처(시각표 페이지)를 단다. 도카이 39개를 조사 에이전트로 모아 넣었다.
적어 둔 노선은 통과 계통을 따로 깔지 않는다(적힌 값이 이미 전부를 센 것이다).

첫 차는 05:00 에 이미 노선 전체를 달리고 있게 운행 시간만큼 앞당겨 깐다.
출발역에서 05:00 에 처음 떠나게 두었을 때는 日豊本線(462km)의 宮崎 에 11시 반에야
첫 차가 와서 宮崎-都城 이 271분으로 나왔다(고친 뒤 75분). 낮 시간대만 보는 간토
검증(0.98)은 그대로다.

## 남은 과제

- **로컬 폴더 이름.** `tokyo-isochrone` -> `rail-isochrone`. VS Code 를 닫고.
- **간사이·간토OSM 을 2026-09-22 에 새 build_rail 로 다시 빌드했다.**
  아래는 그 전에 시뮬레이션(같은 PBF 로 HEAD 코드와 새 코드를 나란히)으로 본 차이.
  간사이는 역 이름 1,614 -> 1,620(関西本線 笠置·大河原·月ケ瀬口·島ヶ原·伊賀上野·
  佐那具·新堂), 노선 길이 합 4,619 -> 4,594km, 남의 역이 빠진다(JR奈良線 의 게이한
  鳥羽街道·伏見稲荷·龍谷大前深草, 阪急京都線 의 祇園四条 등). 간토OSM 은 노선 길이 합
  6,059 -> 6,061km 로 거의 같고 京急本線 에 泉岳寺, 東北本線 에 東京, 常磐線 에 上野 가
  붙는다. 흠이 둘 있다. 新線新宿 가 권역에서 빠진다(예전에도 남의 특급 계통에 잘못
  붙은 자리뿐이었다). 京急本線 끝에 久里浜線 의 京急久里浜 가 붙는다(실제로는
  堀ノ内 에서 갈라진다).
- **나이브 모델이 도카이·간사이에서 느리다.** 간토는 실제 시각표와 역 쌍 251개를
  견주면 소요 시간 비가 중앙 1.00(사분위 0.92~1.07)으로 맞는다. 대략의 실제 값으로
  만든 대표 구간표로는 도카이 1.56배, 간사이 1.21배다. 배차 규칙(역 간격 4km 규칙,
  노선 한 벌을 가장 드문 구간 배차로 까는 것)을 바꿔 봐도 도카이는 1.55배로 거의
  안 움직였다. 원인은 셋이 섞여 있다. 통과 열차 속도(나고야-기후 신쾌속 모델 25분,
  실제 19분. 간토로 맞춘 84.8km/h), 급행 배차(近鉄 급행이 드물다), 직통(名鉄犬山線
  나고야-이누야마 직통이 39분에 한 대. 끝 구간 등급이 노선 전체 배차를 정한다).
  권역별 속도·배차 값을 region.json 에 두고 실제 시각표 몇 노선으로 맞추는 것이
  다음 단계다.
- **도카이 위키 표를 못 읽은 노선.** 近鉄大阪線 은 권역 안이 미에 구간뿐이라
  겹침 12% 로 안 붙었다(그 구간 급행 패턴이 없다). 関西本線(快速みえ)·
  東海道線(静岡地区)·伊豆急行線·지선 대부분은 표 모양이 달라 못 읽었다.
- **권역을 필요할 때만 올리기.** 서버는 권역마다 실제 RAM 350~400MB 를 쓴다
  (대부분 보행망). 권역이 늘면 처음 요청할 때 올리고 오래 안 쓰면 내리는 편이
  낫다. 뜰 때 커밋 메모리가 두 배로 보이는 것은 OpenBLAS 가 스레드마다 잡는
  예약분이라 실제 RAM 과 무관하다(`OPENBLAS_NUM_THREADS=1` 로 줄어든다).
- **되꺾임 10곳.** 선로 자체가 그런 곳이 4곳이고(한노 스위치백 포함) 나머지는
  이음매 아티팩트다. 세타가야선 산겐자야, 도조선 아사카–와코시, 산요 본선
  니시아카시가 두 권역에 똑같이 나온다.
- **구간 선형을 못 찾는 구간.** 간토 28, 간토OSM 12, 간사이 4. 거의 다
  지하철과 1km 안쪽 짧은 구간이다.
- **중간역 없는 지선은 도식 선형이 남는다.** 무사시노선 오미야 지선
  (北朝霞–大宮)이 직선 대비 +27% 다. 실제 경로가 니시우라와 지선을 돌아서
  그 정도가 맞을 수도 있다. 눈으로 확인해야 한다.
- **나카노시마선이 なにわ橋 에서 끝난다.** 天満橋 는 게이한 본선 역이라
  `fill_missing` 후보에서 빠진다. 경로 계산은 997m 걸어서 환승으로 이어져
  구멍이 없다. 종점에 한해 예외를 둘지 정하지 않았다.
- **센리선이 3역짜리 토막으로 보인다.** OSM 이 淡路-北千里 를 교토 본선
  직통 계통 관계 안에 넣었다. 노선별로 쪼개는 것을 만들었다가 뺐다. 같은
  열차가 淡路 에서 직통하는 것이라 쪼개면 실제 운행과 더 멀어진다.
- **碓氷峠 관광 트롯코가 경로 그래프에 남는다.** 위 "지도에 안 그리는 것".
- **죽은 코드.** `geometry.own_pieces`(64줄), `build_geometry.pick_better`
  (41줄), `build_naive.py` 의 `import sys`, `tests/conftest.py` 의 `import numpy`.
- **중복 헬퍼.** `_pbf_list`(build_rail·build_walk), `to_csr`(build_track·
  build_walk), `norm`·`wikitext`(build_colors·build_express), `operator_of`
  (build_colors·operators — 하는 일이 다를 수 있어 확인 필요), 길이 재는 것
  넷(`geometry._length_m`, `build_track._length_m`, `region._path_len_m`,
  `region._dist_m`).
- **진단 코드.** `build_track.py` 의 `PROBE`·`PROBE_WAYS` 환경변수. 한 구간이
  왜 그 길로 갔는지, 역 옆 선로 이름이 뭔지 찍는다. 東北本線（埼京線） 을
  이걸로 찾았다. 남길지 정하지 않았다.
- **노선 id 슬러그가 불안정하다.** 사업자 이름에서 ASCII 만 남겨서 OSM 권역
  노선이 거의 다 `OSM.JR___` 처럼 밑줄 개수로 구별된다. 추출본이 갱신되면
  id 가 조용히 옮겨 붙는다. `_operator_rank` 도 이 앞머리로 회사를 보려다
  OSM 권역에서는 판별을 전혀 못 한다.
- **화면의 출처 표시가 고정되어 있다.** 시각표가 없는 권역을 골라도
  "시각표 출처 mini-tokyo-3d" 가 뜨고, 위키백과 CC BY-SA 표기가 안 나온다.
- **`build_walk.py` 가 해안선 way 를 중복 제거하지 않는다.** 추출본 두 개가
  겹치는 권역에서 공유 way 가 두 번 들어간다.
