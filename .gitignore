# 구글 지도 API 키. 절대 올리지 않는다.
config.json

# 내려받거나 빌드해서 만드는 데이터 (680 MB).
# README 의 순서대로 돌리면 그대로 재현된다.
data/osm/
data/regions/*/raw/
data/regions/*/walk/
data/regions/*/stops.json
data/regions/*/graph-*.npz

# 권역 설정 자체는 코드에 가깝다. 이것만 남긴다.
!data/regions/*/region.json

__pycache__/
*.pyc
.pytest_cache/
